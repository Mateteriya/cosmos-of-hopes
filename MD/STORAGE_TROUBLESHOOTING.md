# 🔧 РЕШЕНИЕ ПРОБЛЕМ С ЗАГРУЗКОЙ ИЗОБРАЖЕНИЙ

## ❌ Проблема: Изображения не загружаются в Storage

Если данные сохраняются в таблицу, но изображения не появляются в Storage, проверьте следующее:

---

## ✅ ШАГ 1: Проверка консоли браузера

1. Откройте приложение в браузере
2. Нажмите **F12** (или правой кнопкой → "Inspect" / "Проверить элемент")
3. Откройте вкладку **"Console"** (Консоль)
4. Попробуйте создать игрушку с изображением
5. Посмотрите, какие сообщения появляются в консоли

**Что искать:**
- Сообщения "Загрузка изображения..."
- Ошибки (красным цветом)
- Сообщения об успешной загрузке

**Скопируйте все сообщения из консоли** и покажите мне - это поможет понять проблему!

---

## ✅ ШАГ 2: Проверка политик Storage

Возможно, политики доступа не созданы или созданы неправильно.

### Проверка:

1. Откройте Supabase → **SQL Editor**
2. Выполните этот запрос:

```sql
SELECT * FROM pg_policies 
WHERE tablename = 'objects' 
AND schemaname = 'storage';
```

3. Должны быть видны политики:
   - "Anyone can upload images"
   - "Anyone can read images"

### Если политик нет - создайте их:

```sql
-- Удаляем старые политики (если есть)
DROP POLICY IF EXISTS "Anyone can upload images" ON storage.objects;
DROP POLICY IF EXISTS "Anyone can read images" ON storage.objects;

-- Создаем новые политики
CREATE POLICY "Anyone can upload images" 
ON storage.objects 
FOR INSERT 
WITH CHECK (bucket_id = 'toy-images');

CREATE POLICY "Anyone can read images" 
ON storage.objects 
FOR SELECT 
USING (bucket_id = 'toy-images');

-- Также нужна политика для обновления (на случай если файл уже существует)
CREATE POLICY "Anyone can update images" 
ON storage.objects 
FOR UPDATE 
USING (bucket_id = 'toy-images');
```

---

## ✅ ШАГ 3: Проверка bucket

1. Откройте Supabase → **Storage**
2. Проверьте:
   - ✅ Bucket `toy-images` существует
   - ✅ Bucket помечен как **Public** (должна быть пометка "Public")
   - ✅ Bucket не пустой (если вы уже пытались загружать)

### Если bucket не Public:

1. Откройте bucket `toy-images`
2. Нажмите на иконку настроек (шестеренка) или "Settings"
3. Включите **"Public bucket"**
4. Сохраните

---

## ✅ ШАГ 4: Проверка RLS (Row Level Security)

Storage использует RLS. Проверьте, что он правильно настроен:

1. Откройте Supabase → **SQL Editor**
2. Выполните:

```sql
-- Проверяем, включен ли RLS для storage.objects
SELECT tablename, rowsecurity 
FROM pg_tables 
WHERE schemaname = 'storage' AND tablename = 'objects';
```

3. Если `rowsecurity = true`, но политик нет - это проблема!

### Решение:

```sql
-- Временно отключаем RLS для тестирования (НЕ для продакшена!)
ALTER TABLE storage.objects DISABLE ROW LEVEL SECURITY;

-- Или создаем политику, которая разрешает всё (для тестирования)
CREATE POLICY "Allow all for toy-images" 
ON storage.objects 
FOR ALL 
USING (bucket_id = 'toy-images')
WITH CHECK (bucket_id = 'toy-images');
```

---

## ✅ ШАГ 5: Проверка API ключей

Убедитесь, что в `.env.local` правильные ключи:

1. Откройте файл `cosmos-of-hopes/.env.local`
2. Проверьте:
   - `NEXT_PUBLIC_SUPABASE_URL` - должен начинаться с `https://`
   - `NEXT_PUBLIC_SUPABASE_ANON_KEY` - длинная строка (JWT токен)

3. **Перезапустите приложение:**
   ```bash
   # Остановите (Ctrl+C)
   # Запустите снова
   npm run dev
   ```

---

## ✅ ШАГ 6: Тестовая загрузка через Supabase UI

Попробуйте загрузить файл вручную через интерфейс Supabase:

1. Откройте Supabase → **Storage** → **toy-images**
2. Нажмите **"Upload file"** (Загрузить файл)
3. Выберите любое изображение
4. Попробуйте загрузить

**Если загрузка через UI работает:**
- Проблема в коде приложения
- Проверьте консоль браузера (шаг 1)

**Если загрузка через UI не работает:**
- Проблема в настройках Supabase
- Проверьте политики (шаг 2) и bucket (шаг 3)

---

## 🔍 ДИАГНОСТИКА: Что проверить в консоли

После попытки загрузки изображения в консоли должны появиться:

```
Загрузка изображения: { filePath: "...", fileName: "...", size: ... }
Изображение загружено: { path: "..." }
Публичная ссылка: https://...
```

**Если видите ошибку:**
- Скопируйте полный текст ошибки
- Покажите мне

**Если не видите никаких сообщений:**
- Возможно, изображение не передается в функцию
- Проверьте, что файл действительно выбран в конструкторе

---

## 🚨 ЧАСТЫЕ ОШИБКИ

### Ошибка: "new row violates row-level security policy"
**Решение:** Создайте политики доступа (шаг 2)

### Ошибка: "Bucket not found"
**Решение:** Проверьте, что bucket `toy-images` создан и назван правильно

### Ошибка: "Invalid API key"
**Решение:** Проверьте ключи в `.env.local` и перезапустите приложение

### Ошибка: "File size exceeds the maximum allowed size"
**Решение:** Supabase по умолчанию ограничивает размер файла. Увеличьте лимит в настройках bucket или уменьшите размер изображения.

---

## 📞 ЕСЛИ НИЧЕГО НЕ ПОМОГЛО

1. **Откройте консоль браузера** (F12)
2. **Скопируйте ВСЕ сообщения** (особенно ошибки)
3. **Сделайте скриншот** страницы Storage в Supabase
4. **Сообщите мне:**
   - Что видно в консоли
   - Какие ошибки
   - Что видно в Storage (пусто или есть файлы)

Я помогу разобраться! 🔧


