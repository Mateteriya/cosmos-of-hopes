# 📱 БЫСТРЫЙ СТАРТ: СЕРВЕР НА ANDROID ПЛАНШЕТЕ

## 🎯 ЦЕЛЬ

Запустить сервер приложения "Вселенная Желаний" на планшете Huawei MediaPad M5 Lite 10 для сбора данных до 1 января 2027 года.

---

## ⚠️ ВАЖНЫЕ ПРЕДУПРЕЖДЕНИЯ

### Риски использования планшета как сервера:

1. **Батарея** - постоянная работа может повредить батарею
2. **Стабильность** - Android может убивать фоновые процессы
3. **Надежность** - нет гарантии работы 24/7 без сбоев
4. **Производительность** - ниже, чем у обычного сервера

### Рекомендация:

**Используйте планшет как ДОПОЛНИТЕЛЬНЫЙ сервер**, а основной оставьте на VPS. Это даст:
- ✅ Надежность (если планшет упадет, основной сервер работает)
- ✅ Резервное копирование данных
- ✅ Возможность тестирования

---

## 🚀 БЫСТРАЯ УСТАНОВКА (5 ШАГОВ)

### Шаг 1: Установить Termux

1. Скачайте **Termux** из F-Droid (рекомендуется) или Google Play
2. Откройте Termux
3. Разрешите доступ к хранилищу при первом запуске

### Шаг 2: Запустить скрипт настройки

В Termux выполните:

```bash
# Скачать скрипт настройки (если есть доступ к интернету)
# ИЛИ скопировать файл android-setup.sh на планшет через USB

# Сделать исполняемым
chmod +x android-setup.sh

# Запустить
./android-setup.sh
```

Скрипт автоматически:
- ✅ Установит Node.js и необходимые инструменты
- ✅ Создаст скрипты автозапуска и мониторинга
- ✅ Настроит автоматическое резервное копирование

### Шаг 3: Скопировать проект на планшет

**Вариант A: Через Git (если есть интернет)**
```bash
cd ~
git clone <ваш-репозиторий> cosmos-of-hopes
```

**Вариант B: Через USB OTG**
1. Подключите USB-флешку к планшету
2. Скопируйте папку `cosmos-of-hopes` на флешку с компьютера
3. В Termux:
```bash
# Создать точку монтирования (если нужно)
mkdir -p ~/usb

# Скопировать файлы
cp -r /path/to/usb/cosmos-of-hopes ~/
```

### Шаг 4: Настроить конфигурацию

```bash
cd ~/cosmos-of-hopes

# Создать файл конфигурации
cp .env.local.example .env.local

# Открыть для редактирования
nano .env.local
```

**Заполните значения:**
```env
NEXT_PUBLIC_SUPABASE_URL=ваш_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=ваш_anon_key
NEXT_PUBLIC_APP_URL=http://ваш-ip-планшета:3000
PORT=3000
```

Сохраните: `Ctrl+O`, `Enter`, `Ctrl+X`

### Шаг 5: Установить и запустить

```bash
# Установить зависимости
npm install

# Собрать приложение
npm run build

# Запустить сервер
npm start
```

**Или использовать скрипт автозапуска:**
```bash
~/start-server.sh
```

---

## ⚙️ НАСТРОЙКА ПЛАНШЕТА (ОБЯЗАТЕЛЬНО!)

### 1. Отключить оптимизацию батареи

1. **Настройки** → **Батарея** → **Оптимизация батареи**
2. Найдите **Termux** в списке
3. Выберите **"Не оптимизировать"**

### 2. Отключить спящий режим

1. **Настройки** → **Экран** → **Спящий режим**
2. Выберите **"Никогда"** (или максимальное время)

### 3. Включить автозапуск

1. **Настройки** → **Приложения** → **Termux**
2. Найдите **"Автозапуск"** или **"Автоматический запуск"**
3. Включите для Termux

### 4. Настроить постоянную зарядку

**ВАЖНО:** Постоянная зарядка может повредить батарею!

**Рекомендации:**
- Используйте зарядное устройство с функцией "защита батареи"
- Или используйте приложение для контроля зарядки (останавливать на 85%)
- Или подключите через USB OTG к внешнему аккумулятору

---

## 📊 ПРОВЕРКА РАБОТЫ

### Проверить, что сервер запущен:

```bash
# Проверить процессы
ps aux | grep node

# Проверить порт
netstat -tuln | grep 3000

# Проверить доступность
curl http://localhost:3000
```

### Просмотр логов:

```bash
# Логи сервера
tail -f ~/server.log

# Логи мониторинга
tail -f ~/server-monitor.log
```

---

## 🔧 ПОЛЕЗНЫЕ КОМАНДЫ

### Управление сервером:

```bash
# Запустить сервер
~/start-server.sh

# Остановить сервер
pkill -f "next start"

# Перезапустить сервер
pkill -f "next start" && sleep 2 && ~/start-server.sh

# Проверить статус
ps aux | grep node
```

### Резервное копирование:

```bash
# Создать бэкап вручную
~/backup-server.sh

# Просмотреть бэкапы
ls -lh ~/backups/
```

### Мониторинг:

```bash
# Использование памяти
free -h

# Использование диска
df -h

# Температура (если доступно)
cat /sys/class/thermal/thermal_zone*/temp
```

---

## 🌐 ДОСТУП ИЗВНЕ

### Вариант 1: Через туннель (ngrok) - ПРОСТОЙ

```bash
# Установить ngrok
pkg install ngrok

# Запустить туннель
ngrok http 3000
```

Скопируйте URL из вывода (например: `https://abc123.ngrok.io`)

### Вариант 2: Через VPS (reverse proxy) - НАДЕЖНЕЕ

На вашем VPS настроить nginx для проксирования на планшет через туннель.

---

## ⚠️ ЧТО ДЕЛАТЬ ПРИ ПРОБЛЕМАХ

### Сервер не запускается:

1. Проверить логи: `tail -f ~/server.log`
2. Проверить, что приложение собрано: `ls -la .next`
3. Пересобрать: `npm run build`
4. Проверить конфигурацию: `cat .env.local`

### Сервер падает:

1. Проверить логи: `tail -f ~/server.log`
2. Проверить память: `free -h`
3. Проверить мониторинг: `tail -f ~/server-monitor.log`
4. Перезапустить: `~/start-server.sh`

### Планшет перезагрузился:

Сервер должен запуститься автоматически при открытии Termux (благодаря настройке в .bashrc).

Если не запустился:
```bash
~/start-server.sh
```

### Интернет пропал:

- Проверьте подключение Wi-Fi или LTE
- Сервер продолжит работать локально
- Данные сохранятся и синхронизируются при восстановлении связи

---

## 📅 ПЛАН ПОДДЕРЖКИ НА ГОД

### Еженедельно:
- [ ] Проверить логи: `tail -20 ~/server.log`
- [ ] Проверить бэкапы: `ls -lh ~/backups/`
- [ ] Проверить доступность: открыть в браузере

### Ежемесячно:
- [ ] Обновить зависимости: `npm update`
- [ ] Пересобрать приложение: `npm run build`
- [ ] Проверить место на диске: `df -h`
- [ ] Очистить старые бэкапы (если нужно)

### При обновлениях:
- [ ] Создать бэкап: `~/backup-server.sh`
- [ ] Обновить код
- [ ] Пересобрать: `npm run build`
- [ ] Перезапустить: `pkill -f "next start" && ~/start-server.sh`

---

## 🎯 ИТОГОВЫЙ ЧЕКЛИСТ

- [ ] Termux установлен и настроен
- [ ] Скрипт настройки выполнен
- [ ] Проект скопирован на планшет
- [ ] .env.local настроен
- [ ] Зависимости установлены (npm install)
- [ ] Приложение собрано (npm run build)
- [ ] Сервер запущен и работает
- [ ] Оптимизация батареи отключена
- [ ] Спящий режим отключен
- [ ] Автозапуск включен
- [ ] Постоянная зарядка настроена
- [ ] Доступ извне настроен (туннель/VPS)
- [ ] Резервное копирование работает
- [ ] Мониторинг работает

---

## 📞 ПОДДЕРЖКА

Если что-то не работает:

1. Проверьте логи: `tail -f ~/server.log`
2. Проверьте процессы: `ps aux | grep node`
3. Перезапустите: `~/start-server.sh`
4. Проверьте конфигурацию: `cat .env.local`

**Помните:** Планшет - это дополнительный сервер. Основной сервер на VPS должен продолжать работать для надежности!

