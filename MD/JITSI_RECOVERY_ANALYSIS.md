# 🔍 Анализ состояния Jitsi и план восстановления

## ❗ Найденные проблемы в коде

### Проблема 1: Нарушение правил React Hooks

**Файл:** `components/rooms/VideoRoom.tsx`

**Строка 61:** `const [isFullscreen, setIsFullscreen] = useState(false);` объявлен **ПОСЛЕ** условного возврата `if (error)`.

**Проблема:** Хуки React должны всегда вызываться в одном порядке и не могут быть условными. Это может вызывать ошибки.

**Исправление:** Переместить все useState в начало компонента, до любых условных возвратов.

---

## 📋 План диагностики (ШАГ 1 - сначала это!)

### Команды для проверки на сервере:

```bash
# 1. Проверить какие пакеты Jitsi установлены
dpkg -l | grep jitsi

# 2. Проверить статус всех сервисов
systemctl status prosody jitsi-videobridge2 jicofo nginx --no-pager

# 3. Проверить конфигурацию nginx для Jitsi
ls -la /etc/nginx/sites-enabled/ | grep -E "(meet|jitsi)"
cat /etc/nginx/sites-enabled/meet.super2026.online.conf 2>/dev/null | head -30

# 4. Проверить конфигурационные файлы Jitsi
ls -la /etc/jitsi/ 2>/dev/null
ls -la /etc/jitsi/meet/ 2>/dev/null
cat /etc/jitsi/meet/meet.super2026.online-config.js 2>/dev/null | head -20

# 5. Проверить переменную окружения
cd /root/cosmos-of-hopes
grep JITSI .env.local 2>/dev/null || echo "Переменная не найдена"

# 6. Проверить доступность Jitsi сервера
curl -I https://meet.super2026.online 2>&1 | head -5

# 7. Проверить логи nginx на ошибки
tail -20 /var/log/nginx/error.log
```

**Выполните эти команды и пришлите результаты!**

---

## 🎯 Варианты восстановления

### ⚡ Вариант 1: Быстрое исправление (рекомендуется начать с этого)

**Когда использовать:** Если Jitsi установлен, но просто не работает или конфликтует с приложением.

**План действий:**

1. **Исправить код:**
   - Исправить порядок useState в `VideoRoom.tsx`
   - Убедиться что код компилируется

2. **Проверить на сервере:**
   - Если конфигурация nginx конфликтует → исправить
   - Если сервисы не запущены → запустить
   - Если конфиг файл отсутствует → создать

3. **Обновить приложение:**
   - Закоммитить исправления
   - На сервере: `git pull`, `npm run build`, `pm2 restart`

**Время:** ~15-20 минут  
**Риск:** Низкий

---

### 🔄 Вариант 2: Полная переустановка Jitsi

**Когда использовать:** Если установка полностью сломана, пакеты в неконсистентном состоянии, или нужно чистое начало.

**План действий:**

1. **Удалить всё:**
   ```bash
   # Удалить пакеты
   apt remove --purge jitsi-meet jitsi-meet-prosody jitsi-meet-web jitsi-meet-web-config jitsi-meet-turnserver jicofo jitsi-videobridge2
   
   # Удалить конфигурации
   rm -rf /etc/jitsi
   rm -rf /etc/prosody/conf.avail/*meet*
   rm -rf /var/lib/prosody/*meet*
   rm -f /etc/nginx/sites-enabled/*meet*
   
   # Очистить debconf
   echo "jitsi-videobridge jitsi-videobridge/jvb-hostname string meet.super2026.online" | debconf-set-selections
   ```

2. **Установить заново:**
   - Следовать инструкции из `JITSI_QUICK_START.md`
   - Использовать правильный домен БЕЗ кавычек

3. **Исправить код** (как в варианте 1)

**Время:** ~30-40 минут  
**Риск:** Средний (нужно быть аккуратным с командами)

---

### 🔙 Вариант 3: Временно вернуться к публичному серверу

**Когда использовать:** Если нужно быстро восстановить работу приложения, без собственного сервера.

**План действий:**

1. **Отключить Jitsi на сервере:**
   ```bash
   # Отключить конфигурацию nginx
   mv /etc/nginx/sites-enabled/meet.super2026.online.conf /etc/nginx/sites-enabled/meet.super2026.online.conf.disabled
   systemctl reload nginx
   
   # Остановить сервисы (опционально)
   systemctl stop prosody jitsi-videobridge2 jicofo
   ```

2. **Удалить переменную окружения:**
   ```bash
   cd /root/cosmos-of-hopes
   # Удалить или закомментировать строку NEXT_PUBLIC_JITSI_SERVER_URL из .env.local
   nano .env.local
   ```

3. **Исправить код** (как в варианте 1)

4. **Обновить приложение**

**Время:** ~5-10 минут  
**Риск:** Очень низкий  
**Результат:** Приложение будет использовать `meet.jit.si` (публичный сервер)

---

## 🎯 Рекомендация

**1. Сначала выполнить диагностику** (команды выше) - это покажет что именно сломалось

**2. В зависимости от результатов:**
   - Если проблема только в коде → Исправить код
   - Если сервисы работают, но есть конфликты → Вариант 1
   - Если всё сломано → Вариант 2
   - Если нужно быстро → Вариант 3

**3. В любом случае:** Исправить ошибку в `VideoRoom.tsx` (переместить useState)

---

**Следующий шаг:** Выполните команды диагностики и пришлите результаты. После этого определим точный план действий.

---

**Дата:** 2025-01-28

