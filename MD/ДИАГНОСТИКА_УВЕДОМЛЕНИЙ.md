# 🔍 Диагностика проблемы с уведомлениями

## Шаг 1: Проверьте HTTPS на сервере

Подключитесь к серверу:

```bash
ssh root@5.129.223.23
```

Проверьте конфигурацию Nginx:

```bash
# Посмотреть конфигурацию
cat /etc/nginx/sites-available/cosmos-of-hopes

# Проверить конфигурацию
nginx -t

# Перезагрузить Nginx
systemctl reload nginx
```

Проверьте, что HTTPS работает:

```bash
# Проверить HTTPS
curl -I https://super2026.online

# Должен вернуть HTTP 200
```

## Шаг 2: Проверьте переменные окружения

Убедитесь, что в `.env.local` есть `NEXT_PUBLIC_VAPID_PUBLIC_KEY`:

```bash
cd /root/cosmos-of-hopes

# Проверить файл
cat .env.local | grep VAPID
```

Должно быть:
```
NEXT_PUBLIC_VAPID_PUBLIC_KEY=ваш_ключ_здесь
```

⚠️ **ВАЖНО**: Переменная должна начинаться с `NEXT_PUBLIC_` - это значит, что она будет доступна в браузере!

## Шаг 3: ПЕРЕСОБЕРИТЕ приложение

**Это критически важно!** После изменения `.env.local` нужно пересобрать:

```bash
cd /root/cosmos-of-hopes

# Пересобрать приложение
npm run build

# Перезапустить
pm2 restart cosmos-of-hopes --update-env
```

## Шаг 4: Проверьте в браузере

1. **Откройте сайт по HTTPS** (не HTTP!):
   - `https://super2026.online`

2. **Откройте консоль браузера** (F12 → Console)

3. **Посмотрите на сообщения:**
   - Если видите `[Push Notifications] VAPID public key not configured` - значит переменная не загружена
   - Если видите `[Push Notifications] Service Worker registered` - значит всё ок

4. **Проверьте Service Worker:**
   - F12 → Application (Приложение)
   - В левом меню → Service Workers
   - Должен быть зарегистрирован Service Worker для `super2026.online`

## Шаг 5: Проверьте доступность Service Worker

Откройте в браузере:
```
https://super2026.online/sw.js
```

Должен открыться JavaScript файл (код Service Worker).

Если открывается HTML страница или ошибка 404 - Service Worker не доступен!

## Типичные проблемы:

### Проблема 1: "VAPID public key not configured"

**Причина:** Переменная окружения не загружена в браузер

**Решение:**
1. Убедитесь, что в `.env.local` есть `NEXT_PUBLIC_VAPID_PUBLIC_KEY=...`
2. **ПЕРЕСОБЕРИТЕ** приложение: `npm run build`
3. Перезапустите: `pm2 restart cosmos-of-hopes --update-env`

### Проблема 2: Service Worker не регистрируется

**Причина:** Сайт открыт по HTTP вместо HTTPS

**Решение:**
1. Убедитесь, что открываете `https://super2026.online` (с S!)
2. Service Worker требует HTTPS

### Проблема 3: Кнопка уведомлений не появляется

**Причина:** Service Worker не зарегистрирован или уже есть подписка

**Решение:**
1. Проверьте консоль браузера на ошибки
2. Проверьте, что Service Worker зарегистрирован (F12 → Application → Service Workers)
3. Очистите кеш браузера (Ctrl+Shift+Delete)

## Быстрая проверка - команды для сервера:

```bash
# 1. Проверить переменные окружения
cd /root/cosmos-of-hopes
cat .env.local | grep NEXT_PUBLIC_VAPID

# 2. Пересобрать (если нужно)
npm run build

# 3. Перезапустить
pm2 restart cosmos-of-hopes --update-env

# 4. Проверить логи
pm2 logs cosmos-of-hopes --lines 50

# 5. Проверить HTTPS
curl -I https://super2026.online
```

