# ✅ Push-уведомления: Настроено и работает!

## Статус

✅ Сервер запущен через PM2
✅ Проверка работает каждые 5 минут
✅ Логи показывают нормальную работу

## Настройка автозапуска

Выполните на сервере:

```bash
# Сохранить текущие процессы PM2
pm2 save

# Настроить автозапуск при перезагрузке сервера
pm2 startup
# Выполните команду, которую выведет PM2 (обычно что-то вроде:
# sudo env PATH=$PATH:/usr/bin pm2 startup systemd -u root --hp /root)
```

## Мониторинг

```bash
# Посмотреть статус
pm2 status

# Посмотреть логи
pm2 logs push-notifications

# Посмотреть последние 50 строк
pm2 logs push-notifications --lines 50

# Остановить (если нужно)
pm2 stop push-notifications

# Перезапустить (если нужно)
pm2 restart push-notifications
```

## Что происходит

1. **Каждые 5 минут** сервер проверяет все подписки
2. **31 декабря в 23:57** (по времени пользователя) - отправляет уведомление о шаре в космос
3. **31 декабря в 22:50** (по времени пользователя) - отправляет уведомление о запуске комнаты
4. **Автоматически** удаляет недействительные подписки

## Когда появятся подписки

Подписки появятся, когда пользователи:
- Нажмут "Включить уведомления" в приложении
- Разрешат уведомления в браузере

Подписки сохраняются в таблице `push_subscriptions` в Supabase.

## Проверка подписок

```bash
# Можно проверить в Supabase Dashboard:
# Table Editor → push_subscriptions
```

## Готово! 🎉

Push-уведомления настроены и работают!

