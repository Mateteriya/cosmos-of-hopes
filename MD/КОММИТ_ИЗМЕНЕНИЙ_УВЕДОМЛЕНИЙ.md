# 🔄 Коммит изменений для исправления кнопки уведомлений

## Что нужно сделать

Изменения в файле `NotificationPromptButton.tsx` не попали в git, поэтому на сервере они не применились.

## Команды для выполнения (на локальной машине):

```bash
cd cosmos-of-hopes

# Добавить измененный файл
git add components/notifications/NotificationPromptButton.tsx

# Закоммитить изменения
git commit -m "Исправление: показ кнопки уведомлений на главной странице"

# Запушить на сервер
git push origin main
```

## После этого на сервере:

```bash
ssh root@5.129.223.23

cd /root/cosmos-of-hopes

# Получить изменения
git pull origin main

# Пересобрать
npm run build

# Перезапустить
pm2 restart cosmos-of-hopes --update-env
```

