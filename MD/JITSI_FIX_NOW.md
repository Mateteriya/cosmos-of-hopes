# 🔧 ИСПРАВЛЕНИЕ JITSI: Пошаговые команды

## 📊 Анализ диагностики

**Хорошие новости:**
- ✅ Jitsi установлен и все сервисы работают
- ✅ Конфигурационные файлы существуют
- ✅ Переменная окружения уже настроена

**Проблемы:**
- ❌ Конфигурация nginx для Jitsi отключена (правильно, так как сломала приложение)
- ❌ SSL сертификат неправильный (curl показывает ошибку SSL)

---

## 🎯 ПЛАН ИСПРАВЛЕНИЯ

### Шаг 1: Проверить конфигурацию nginx для Jitsi

```bash
# Посмотреть содержимое конфигурации
cat /etc/nginx/sites-available/meet.super2026.online.conf
```

**Что проверить:**
- Должно быть `server_name meet.super2026.online;` (ТОЛЬКО meet, НЕ super2026.online)
- Не должно быть блоков `if`, которые перехватывают другие домены

**Пришлите вывод этой команды**, чтобы я проверил конфигурацию.

---

### Шаг 2: Исправить SSL сертификат

Ошибка SSL означает, что сертификат не подходит для домена `meet.super2026.online`.

**Вариант A: Если сертификат Let's Encrypt существует, но неправильный**

```bash
# Проверить существующие сертификаты
certbot certificates

# Удалить неправильный сертификат для meet.super2026.online (если есть)
certbot delete --cert-name meet.super2026.online

# Получить новый сертификат
certbot --nginx -d meet.super2026.online
```

**Вариант B: Если нужно использовать self-signed (временно)**

Можно временно отключить проверку SSL, но лучше использовать Let's Encrypt.

---

### Шаг 3: Включить конфигурацию nginx (ПОСЛЕ проверки Шага 1!)

**ВАЖНО:** Включаем конфигурацию ТОЛЬКО если она правильная (только meet.super2026.online).

```bash
# Создать симлинк с префиксом zzz- (загружается последней)
ln -sf /etc/nginx/sites-available/meet.super2026.online.conf /etc/nginx/sites-enabled/zzz-meet.super2026.online.conf

# Проверить синтаксис
nginx -t

# Если ошибок нет - перезагрузить
systemctl reload nginx
```

---

### Шаг 4: Проверка изоляции доменов

**После включения конфигурации ОБЯЗАТЕЛЬНО проверить:**

```bash
# 1. Основной домен должен работать (ваше приложение)
curl -I -H "Host: super2026.online" http://localhost 2>&1 | head -5

# 2. Jitsi домен должен работать (Jitsi)
curl -I -H "Host: meet.super2026.online" http://localhost 2>&1 | head -5
```

**Если основной домен сломался** - сразу отключите конфигурацию Jitsi:
```bash
rm /etc/nginx/sites-enabled/zzz-meet.super2026.online.conf
systemctl reload nginx
```

---

### Шаг 5: Тест работы Jitsi

```bash
# Проверить доступность через HTTPS (после исправления SSL)
curl -I https://meet.super2026.online 2>&1 | head -5

# Должен вернуть HTTP/1.1 200 (без ошибки SSL)
```

---

## 🔍 СНАЧАЛА: Проверка конфигурации

**Выполните ЭТУ команду первым делом:**

```bash
cat /etc/nginx/sites-available/meet.super2026.online.conf
```

**Пришлите полный вывод** - я проверю конфигурацию и скажу, можно ли её безопасно включать.

---

## ⚠️ ВАЖНО

1. **НЕ включайте конфигурацию** пока я не проверю её содержимое
2. **SSL сертификат нужно исправить** перед включением
3. **После включения обязательно проверьте** основной домен

---

**Дата:** 2025-01-28

