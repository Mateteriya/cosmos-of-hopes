# 🚨 СРОЧНОЕ ИСПРАВЛЕНИЕ СЕРВЕРА

## Проблема
- Приложение перезапускалось 417 раз
- Ошибки "Could not find a production build"
- Ошибки с JSON parsing и clientReferenceManifest
- Яндекс браузер не может загрузить приложение

## Решение

### Шаг 1: Полная остановка и очистка

```bash
# Остановите все процессы
pm2 stop all
pm2 delete all

# Удалите старый билд
cd ~/cosmos-of-hopes
rm -rf .next
rm -rf node_modules/.cache
```

### Шаг 2: Проверьте переменные окружения

```bash
# Убедитесь, что .env.local существует и содержит все нужные переменные
cat .env.local | grep NEXT_PUBLIC
```

Должны быть:
- `NEXT_PUBLIC_SUPABASE_URL`
- `NEXT_PUBLIC_SUPABASE_ANON_KEY`
- `NEXT_PUBLIC_VAPID_PUBLIC_KEY`
- `NEXT_PUBLIC_APP_URL`
- `NEXT_PUBLIC_JITSI_SERVER_URL`

### Шаг 3: Пересоберите приложение

```bash
# Убедитесь, что вы в правильной директории
cd ~/cosmos-of-hopes

# Получите последние изменения
git pull

# Установите зависимости (если нужно)
npm install

# Очистите кеш
rm -rf .next
rm -rf node_modules/.cache

# Соберите приложение
npm run build
```

### Шаг 4: Проверьте билд

```bash
# Убедитесь, что билд создан
ls -la .next/

# Должны быть папки:
# - server/
# - static/
# - BUILD_ID
```

### Шаг 5: Запустите приложение заново

```bash
# Запустите с правильными переменными окружения
pm2 start npm --name "cosmos-of-hopes" -- start

# Или если используете ecosystem файл:
# pm2 start ecosystem.config.js
```

### Шаг 6: Проверьте статус

```bash
# Проверьте, что приложение запущено
pm2 status

# Проверьте логи (не должно быть ошибок)
pm2 logs cosmos-of-hopes --lines 20

# Проверьте использование ресурсов
pm2 monit
```

## Если проблема сохраняется:

### Вариант 1: Используйте ecosystem файл

Создайте `ecosystem.config.js`:

```javascript
module.exports = {
  apps: [{
    name: 'cosmos-of-hopes',
    script: 'npm',
    args: 'start',
    cwd: '/root/cosmos-of-hopes',
    env: {
      NODE_ENV: 'production',
      PORT: 3000
    },
    error_file: '/root/.pm2/logs/cosmos-of-hopes-error.log',
    out_file: '/root/.pm2/logs/cosmos-of-hopes-out.log',
    log_date_format: 'YYYY-MM-DD HH:mm:ss Z',
    merge_logs: true,
    autorestart: true,
    max_restarts: 10,
    min_uptime: '10s',
    max_memory_restart: '500M'
  }]
};
```

Затем:
```bash
pm2 start ecosystem.config.js
pm2 save
```

### Вариант 2: Проверьте права доступа

```bash
# Убедитесь, что у пользователя есть права
chown -R $USER:$USER ~/cosmos-of-hopes
chmod -R 755 ~/cosmos-of-hopes
```

### Вариант 3: Проверьте версию Node.js

```bash
node --version  # Должна быть 18.x или 20.x
npm --version
```

Если версия старая:
```bash
# Установите nvm
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash
source ~/.bashrc

# Установите Node.js 20
nvm install 20
nvm use 20
```

## Для Яндекс браузера:

После исправления сервера, попросите пользователей:

1. **Очистить кеш полностью:**
   - Настройки → Дополнительно → Очистить данные
   - Выбрать "За все время"
   - Отметить все пункты
   - Нажать "Очистить"

2. **Отключить расширения:**
   - Настройки → Расширения
   - Отключить все расширения временно

3. **Проверить в режиме инкогнито:**
   - Откройте сайт в режиме инкогнито
   - Если работает - проблема в расширениях или кеше

## Мониторинг после исправления:

```bash
# Следите за логами
pm2 logs cosmos-of-hopes --lines 50

# Проверьте, что нет постоянных перезапусков
pm2 monit

# Проверьте доступность
curl -I https://super2026.online
```

## Критично:

Если приложение продолжает перезапускаться:
1. Проверьте логи на конкретные ошибки
2. Убедитесь, что все переменные окружения установлены
3. Проверьте, что порт 3000 не занят другим процессом
4. Проверьте использование памяти (не должно превышать лимит)

