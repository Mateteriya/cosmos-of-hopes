# ⚡ БЫСТРАЯ НАСТРОЙКА SUPABASE

## 🎯 Вы уже создали проект и получили API ключ? Отлично!

Теперь нужно выполнить всего 2 шага:

---

## 📝 ШАГ 1: Выполнить SQL схему (2 минуты)

1. Откройте ваш проект в Supabase
2. В левом меню нажмите **"SQL Editor"**
3. Нажмите **"New query"** (Новый запрос)
4. Откройте файл `cosmos-of-hopes/supabase/schema.sql` на вашем компьютере
5. Скопируйте **ВСЁ** содержимое файла (Ctrl+A, Ctrl+C)
6. Вставьте в SQL Editor в Supabase (Ctrl+V)
7. Нажмите кнопку **"Run"** (или Ctrl+Enter)
8. Должно появиться зеленое сообщение "Success"

**Готово!** Таблицы созданы.

---

## 📦 ШАГ 2: Создать Storage bucket (3 минуты)

### Часть А: Создание bucket

1. В Supabase откройте **"Storage"** (в левом меню)
2. Нажмите **"New bucket"** (кнопка вверху справа)
3. В поле **"Name"** введите: `toy-images`
4. **Включите** переключатель **"Public bucket"** (должен быть включен!)
5. Нажмите **"Create bucket"**

### Часть Б: Настройка доступа

1. Вернитесь в **"SQL Editor"**
2. Создайте новый запрос (New query)
3. Скопируйте и вставьте этот код:

```sql
-- Политики для Storage
CREATE POLICY "Anyone can upload images" 
ON storage.objects FOR INSERT 
WITH CHECK (bucket_id = 'toy-images');

CREATE POLICY "Anyone can read images" 
ON storage.objects FOR SELECT 
USING (bucket_id = 'toy-images');
```

4. Нажмите **"Run"**

**Готово!** Storage настроен.

---

## ✅ ПРОВЕРКА

1. Откройте **"Table Editor"** в Supabase
2. Вы должны увидеть таблицы: `toys`, `rooms`, `user_profiles`, `room_members`
3. Откройте **"Storage"**
4. Вы должны увидеть bucket `toy-images` с пометкой "Public"

Если всё видно - **настройка завершена!** 🎉

---

## 📄 Подробные инструкции

Если что-то непонятно, смотрите файл `SUPABASE_SETUP_DETAILED.md` - там всё расписано пошагово с объяснениями.

---

**Следующий шаг:** Создайте файл `.env.local` с вашими API ключами (см. `SUPABASE_SETUP_DETAILED.md`, шаг 4)


