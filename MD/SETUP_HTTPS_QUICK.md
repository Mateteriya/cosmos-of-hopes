# 🔒 Быстрая настройка HTTPS для super2026.online

## Время: 15-20 минут

## Шаг 1: Установить Certbot (если не установлен)

```bash
# Обновить пакеты
apt update

# Установить certbot и плагин для nginx
apt install -y certbot python3-certbot-nginx
```

## Шаг 2: Получить SSL сертификат

```bash
# Получить сертификат для основного домена
certbot --nginx -d super2026.online -d www.super2026.online

# Certbot спросит:
# - Email для уведомлений (введите ваш email)
# - Согласие с условиями (Y)
# - Редирект HTTP на HTTPS (выберите 2 - Redirect, чтобы работало и HTTP и HTTPS)
```

## Шаг 3: Проверить автоматическое обновление

```bash
# Certbot автоматически настроит обновление сертификатов
# Проверить, что таймер работает
systemctl status certbot.timer

# Должно быть: active (waiting)
```

## Шаг 4: Проверить работу

```bash
# Проверить HTTP (должен редиректить на HTTPS)
curl -I http://super2026.online

# Проверить HTTPS
curl -I https://super2026.online

# Должен вернуть HTTP 200
```

## Шаг 5: Если нужно, настроить редирект вручную

Если Certbot настроил редирект, но вы хотите, чтобы работало и HTTP и HTTPS:

```bash
# Отредактировать конфигурацию
nano /etc/nginx/sites-available/cosmos-of-hopes
```

Убедитесь, что есть два server блока:
- Один для HTTP (порт 80) - может редиректить или проксировать
- Один для HTTPS (порт 443) - проксирует на localhost:3000

## Проверка после настройки

1. Откройте в браузере: `https://super2026.online`
2. Должен быть зеленый замочек (SSL работает)
3. Приложение должно открываться

## Важно

- Сертификат Let's Encrypt действителен 90 дней
- Certbot автоматически обновляет сертификат
- Не нужно ничего делать вручную после настройки

## Если возникли проблемы

```bash
# Проверить логи certbot
tail -50 /var/log/letsencrypt/letsencrypt.log

# Проверить конфигурацию nginx
nginx -t

# Перезагрузить nginx
systemctl reload nginx
```

