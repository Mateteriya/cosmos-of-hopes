# 🔐 Настройка системы регистрации пользователей

## ✅ Что создано

1. **Компоненты авторизации:**
   - `AuthButton` - кнопка регистрации/входа в правом верхнем углу
   - `AuthModal` - модальное окно для регистрации и входа

2. **Утилиты:**
   - `lib/auth.ts` - функции для работы с Supabase Auth
   - `lib/userId.ts` - обновлена для поддержки авторизованных пользователей
   - `lib/userMigration.ts` - миграция данных при регистрации

3. **Миграция базы данных:**
   - `supabase/migration_add_user_registration.sql` - таблица для миграции данных

## 🚀 Настройка (5 минут)

### Шаг 1: Применить миграцию в Supabase

1. Откройте Supabase Dashboard → SQL Editor
2. Скопируйте содержимое файла `supabase/migration_add_user_registration.sql`
3. Вставьте и выполните (Run)

### Шаг 2: Включить Email авторизацию в Supabase

1. Откройте Supabase Dashboard → Authentication → Providers
2. Убедитесь, что **Email** включен
3. (Опционально) Настройте шаблоны email для подтверждения

### Шаг 3: Настроить URL перенаправления

1. Supabase Dashboard → Authentication → URL Configuration
2. Добавьте в **Redirect URLs**:
   - `http://localhost:3000`
   - `https://super2026.online`
   - `https://super2026.online/**`

### Шаг 4: Перезапустить приложение

```bash
# На сервере
pm2 restart cosmos-of-hopes
```

## 📋 Как это работает

### Для новых пользователей:
1. Пользователь нажимает "Регистрация" (правый верхний угол)
2. Заполняет email и пароль
3. После регистрации автоматически входит
4. Может создавать шары и комнаты - они будут привязаны к его аккаунту

### Для существующих пользователей:
1. Пользователь регистрируется
2. При первом входе автоматически мигрируются его данные:
   - Шары на ёлке
   - Созданные комнаты
   - Подписки на уведомления
3. После миграции может использовать данные с любого устройства

### Без регистрации:
- Пользователь может продолжать работать анонимно
- Данные сохраняются в localStorage (только в этом браузере)
- При регистрации данные автоматически переносятся

## 🔄 Миграция данных

При регистрации/входе:
- Автоматически ищутся шары и комнаты со старым ID (из localStorage)
- Они привязываются к новому ID (из Supabase Auth)
- Старый ID удаляется из localStorage
- Пользователь видит сообщение о количестве перенесенных данных

## ✅ Готово!

Система регистрации работает! Пользователи могут:
- Регистрироваться через email/пароль
- Входить в свой аккаунт
- Получать доступ к данным с любого устройства
- Автоматически переносить существующие данные

