# 🔧 Простое восстановление Jitsi

## ✅ Что уже готово

1. ✅ **Код исправлен** - ошибка в VideoRoom.tsx исправлена
2. ✅ **Пакеты установлены** - все пакеты Jitsi на месте
3. ✅ **Сервисы работают** - prosody, jitsi-videobridge2, jicofo запущены
4. ✅ **Конфигурация Jitsi есть** - файлы в `/etc/jitsi/meet/`
5. ✅ **SSL сертификаты есть** - Let's Encrypt сертификаты установлены
6. ✅ **Переменная окружения** - установлена правильно

## ❌ Проблема

Конфигурация nginx для Jitsi отсутствует в `sites-enabled/`.

---

## 🔧 Решение (самый простой способ)

### Шаг 1: Восстановить стандартную конфигурацию через пакет

```bash
# Переустановить конфигурацию (восстановит стандартную конфигурацию nginx)
dpkg-reconfigure jitsi-meet-web-config
```

При запросе домена введите: `meet.super2026.online` (БЕЗ КАВЫЧЕК!)

### Шаг 2: Проверить что конфигурация создалась

```bash
# Проверить sites-available
ls -la /etc/nginx/sites-available/ | grep -E "(jitsi|meet)"

# Проверить sites-enabled
ls -la /etc/nginx/sites-enabled/ | grep -E "(jitsi|meet)"
```

### Шаг 3: Если конфигурация в sites-available, но не в sites-enabled

```bash
# Найти имя файла
FILE=$(ls /etc/nginx/sites-available/ | grep -E "(jitsi|meet)" | head -1)

# Создать симлинк
if [ -n "$FILE" ]; then
    ln -s /etc/nginx/sites-available/$FILE /etc/nginx/sites-enabled/$FILE
    echo "Симлинк создан: $FILE"
fi
```

### Шаг 4: Проверить конфигурацию

```bash
nginx -t
```

### Шаг 5: Перезагрузить nginx

```bash
systemctl reload nginx
```

### Шаг 6: Проверить работу

```bash
curl -I https://meet.super2026.online 2>&1 | head -10
```

Откройте в браузере: `https://meet.super2026.online`

---

## 🔄 Обновить приложение на сервере

После восстановления nginx:

```bash
cd /root/cosmos-of-hopes

# Получить исправления кода
git pull origin main

# Пересобрать
npm run build

# Перезапустить
pm2 restart cosmos-of-hopes --update-env
```

---

**Дата:** 2025-01-28

