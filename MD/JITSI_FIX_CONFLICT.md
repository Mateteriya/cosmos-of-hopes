# 🔧 Исправление конфликта конфигураций nginx

## ⚠️ Проблема

nginx показывает предупреждения:
```
conflicting server name "meet.super2026.online" on 0.0.0.0:443, ignored
conflicting server name "meet.super2026.online" on 0.0.0.0:80, ignored
```

Это означает, что конфигурация для `meet.super2026.online` включена в нескольких местах.

Также certbot добавил конфигурацию SSL для meet.super2026.online в файл `00-cosmos-of-hopes`, что создает конфликт.

---

## 🔍 Диагностика

Выполните на сервере:

```bash
# 1. Проверить все активные конфигурации
ls -la /etc/nginx/sites-enabled/

# 2. Найти все упоминания meet.super2026.online в активных конфигурациях
grep -r "server_name.*meet.super2026.online" /etc/nginx/sites-enabled/

# 3. Проверить, что certbot добавил в cosmos-of-hopes
grep -A 10 -B 5 "meet.super2026.online" /etc/nginx/sites-enabled/00-cosmos-of-hopes
```

---

## ✅ Исправление

### Шаг 1: Удалить конфигурацию meet из cosmos-of-hopes

Certbot добавил конфигурацию SSL для meet.super2026.online в основной файл. Нужно её удалить:

```bash
# Открыть файл
nano /etc/nginx/sites-available/cosmos-of-hopes

# НАЙТИ и УДАЛИТЬ блоки, которые содержат:
# - server_name meet.super2026.online
# - listen 443 ssl;
# - ssl_certificate для meet.super2026.online
```

**В файле cosmos-of-hopes должно остаться ТОЛЬКО:**
```nginx
server {
    listen 80;
    server_name super2026.online www.super2026.online;
    # ... редирект на HTTPS или proxy_pass ...
}

server {
    listen 443 ssl http2;
    server_name super2026.online www.super2026.online;
    # ... SSL для super2026.online (НЕ meet!) ...
    # ... proxy_pass на localhost:3000 ...
}
```

**НЕ должно быть:**
```nginx
server {
    server_name meet.super2026.online;  # ❌ УДАЛИТЬ!
    # ...
}
```

---

### Шаг 2: Обновить конфигурацию Jitsi для использования правильного SSL сертификата

Certbot создал новый сертификат по пути `/etc/letsencrypt/live/meet.super2026.online-0001/`, но конфигурация Jitsi указывает на старый путь.

Нужно обновить конфигурацию:

```bash
# Открыть конфигурацию Jitsi
nano /etc/nginx/sites-available/meet.super2026.online.conf

# Найти строки:
# ssl_certificate /etc/jitsi/meet/meet.super2026.online.crt;
# ssl_certificate_key /etc/jitsi/meet/meet.super2026.online.key;

# Заменить на:
# ssl_certificate /etc/letsencrypt/live/meet.super2026.online-0001/fullchain.pem;
# ssl_certificate_key /etc/letsencrypt/live/meet.super2026.online-0001/privkey.pem;
```

Или использовать более универсальный путь (certbot обычно создает симлинк):

```bash
# Проверить симлинк
ls -la /etc/letsencrypt/live/ | grep meet

# Если есть meet.super2026.online (без -0001), использовать его
# Если только meet.super2026.online-0001, использовать его
```

---

### Шаг 3: Проверить конфигурацию

```bash
# Проверить синтаксис
nginx -t

# Должно быть БЕЗ предупреждений о конфликтах
```

---

### Шаг 4: Перезагрузить nginx

```bash
systemctl reload nginx
```

---

### Шаг 5: Финальная проверка

```bash
# 1. Основной домен (должен показывать приложение)
curl -I https://super2026.online 2>&1 | head -5

# 2. Jitsi домен (должен показывать Jitsi)
curl -I https://meet.super2026.online 2>&1 | head -5

# 3. Проверить, что нет конфликтов
nginx -t
```

---

## 📝 Краткий план действий

1. ✅ Выполнить диагностику (команды выше)
2. ✅ Удалить блоки meet.super2026.online из cosmos-of-hopes
3. ✅ Обновить пути SSL сертификатов в конфигурации Jitsi
4. ✅ Проверить `nginx -t` (должно быть без конфликтов)
5. ✅ Перезагрузить nginx
6. ✅ Проверить работу обоих доменов

---

**Дата:** 2025-12-30

