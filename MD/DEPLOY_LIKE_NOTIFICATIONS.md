# 📱 Полная инструкция: Деплой уведомлений о лайках

## ⚠️ ВАЖНО: Замена VAPID ключей (ОБЯЗАТЕЛЬНО!)

**VAPID ключи были скомпрометированы и должны быть заменены перед деплоем!**

### Шаг 0.1: Генерация новых VAPID ключей

```bash
# Установите web-push (если еще не установлен)
npm install -g web-push

# Сгенерируйте НОВЫЕ ключи
web-push generate-vapid-keys
```

**Скопируйте оба ключа!** Вы получите что-то вроде:
```
Public Key:
BKQO-oVx2u2Dnz38U7RwbRvOnxNGb9QI6JP9_PmWZZ94D4q5NpdqLXmwEFYlH9IpJiP1eZEZfvV84iIjN7smOnA

Private Key:
okPEY6b0ljhnrJ9bhc79FaIGSddOzdRHb2yqrWaNRro
```

### Шаг 0.2: Удаление старых ключей из Supabase Secrets

1. Откройте [Supabase Dashboard](https://app.supabase.com)
2. Выберите ваш проект
3. Перейдите в **Settings** → **Edge Functions** → **Secrets**
4. Найдите и **УДАЛИТЕ** старые секреты:
   - `VAPID_PUBLIC_KEY` (старый)
   - `VAPID_PRIVATE_KEY` (старый)
   - `VAPID_EMAIL` (можно оставить, если email правильный)

### Шаг 0.3: Добавление новых ключей в Supabase Secrets

#### Через Dashboard:
1. В том же разделе **Secrets** нажмите **Add new secret**
2. Добавьте каждый ключ отдельно:

   **Secret 1:**
   - Name: `VAPID_PUBLIC_KEY`
   - Value: `ваш_НОВЫЙ_public_key_из_шага_0.1`

   **Secret 2:**
   - Name: `VAPID_PRIVATE_KEY`
   - Value: `ваш_НОВЫЙ_private_key_из_шага_0.1`

   **Secret 3:**
   - Name: `VAPID_EMAIL`
   - Value: `mailto:your-email@example.com` (замените на ваш email)

#### Через CLI:
```bash
# Установите переменные окружения (замените на ваши НОВЫЕ ключи)
export VAPID_PUBLIC_KEY="ваш_НОВЫЙ_public_key"
export VAPID_PRIVATE_KEY="ваш_НОВЫЙ_private_key"
export VAPID_EMAIL="mailto:your-email@example.com"

# Добавьте секреты в Supabase
supabase secrets set VAPID_PUBLIC_KEY="$VAPID_PUBLIC_KEY"
supabase secrets set VAPID_PRIVATE_KEY="$VAPID_PRIVATE_KEY"
supabase secrets set VAPID_EMAIL="$VAPID_EMAIL"
```

### Шаг 0.4: Обновление ключей в .env.local на сервере

**ВАЖНО:** Также нужно обновить `NEXT_PUBLIC_VAPID_PUBLIC_KEY` в `.env.local` на сервере!

1. Подключитесь к серверу
2. Откройте `.env.local`:
```bash
nano ~/cosmos-of-hopes/.env.local
```

3. Замените старый ключ на новый:
```env
NEXT_PUBLIC_VAPID_PUBLIC_KEY=ваш_НОВЫЙ_public_key_из_шага_0.1
```

4. Сохраните файл (Ctrl+O, Enter, Ctrl+X)
5. Перезапустите приложение:
```bash
pm2 restart cosmos-of-hopes --update-env
```

### Шаг 0.5: Переподписка пользователей (опционально)

**Внимание:** После замены VAPID ключей пользователям может потребоваться переподписаться на уведомления.

Можно либо:
- Попросить пользователей нажать кнопку "Включить уведомления" снова
- Или оставить как есть - новые подписки будут работать с новыми ключами

---

## Шаг 1: Установка Supabase CLI (если еще не установлен)

### Windows (PowerShell):
```powershell
# Установка через npm (если установлен Node.js)
npm install -g supabase

# Или через Scoop (если установлен)
scoop bucket add supabase https://github.com/supabase/scoop-bucket.git
scoop install supabase
```

### Linux/Mac:
```bash
# Через npm
npm install -g supabase

# Или через Homebrew (Mac)
brew install supabase/tap/supabase
```

### Проверка установки:
```bash
supabase --version
```

---

## Шаг 2: Вход в Supabase

```bash
# Войдите в свой аккаунт Supabase
supabase login
```

Откроется браузер, где нужно авторизоваться через GitHub или email.

---

## Шаг 3: Связывание проекта с Supabase

```bash
# Перейдите в папку проекта
cd cosmos-of-hopes

# Свяжите проект с вашим Supabase проектом
supabase link --project-ref ваш-project-ref
```

**Где найти project-ref:**
1. Откройте [Supabase Dashboard](https://app.supabase.com)
2. Выберите ваш проект
3. Перейдите в **Settings** → **General**
4. Скопируйте **Reference ID** (например: `pjvbiblalapcbgwpojvm`)

**Или используйте URL проекта:**
```bash
supabase link --project-ref pjvbiblalapcbgwpojvm
```

---

## Шаг 4: Проверка VAPID ключей в Supabase Secrets

**ВАЖНО:** Если вы уже выполнили Шаг 0 (замену ключей), этот шаг можно пропустить - ключи уже настроены!

### 4.1. Проверка существующих ключей:

1. Откройте [Supabase Dashboard](https://app.supabase.com)
2. Выберите ваш проект
3. Перейдите в **Settings** → **Edge Functions** → **Secrets**
4. Проверьте, что есть следующие секреты:
   - `VAPID_PUBLIC_KEY` (должен быть НОВЫЙ ключ после замены!)
   - `VAPID_PRIVATE_KEY` (должен быть НОВЫЙ ключ после замены!)
   - `VAPID_EMAIL`

### 4.2. Если ключей нет - сгенерируйте их (только если не выполнили Шаг 0):

#### Вариант 1: Через npm (локально):
```bash
# Установите web-push (если еще не установлен)
npm install -g web-push

# Сгенерируйте ключи
web-push generate-vapid-keys
```

Вы получите что-то вроде:
```
Public Key:
BKQO-oVx2u2Dnz38U7RwbRvOnxNGb9QI6JP9_PmWZZ94D4q5NpdqLXmwEFYlH9IpJiP1eZEZfvV84iIjN7smOnA

Private Key:
okPEY6b0ljhnrJ9bhc79FaIGSddOzdRHb2yqrWaNRro
```

#### Вариант 2: Через скрипт проекта:
```bash
cd cosmos-of-hopes
npm run generate-vapid-keys
```

### 4.3. Добавление ключей в Supabase Secrets:

#### Через Dashboard:
1. Откройте **Settings** → **Edge Functions** → **Secrets**
2. Нажмите **Add new secret**
3. Добавьте каждый ключ отдельно:

   **Secret 1:**
   - Name: `VAPID_PUBLIC_KEY`
   - Value: `ваш_public_key_из_шага_4.2`

   **Secret 2:**
   - Name: `VAPID_PRIVATE_KEY`
   - Value: `ваш_private_key_из_шага_4.2`

   **Secret 3:**
   - Name: `VAPID_EMAIL`
   - Value: `mailto:your-email@example.com` (замените на ваш email)

#### Через CLI:
```bash
# Установите переменные окружения (замените на ваши ключи)
export VAPID_PUBLIC_KEY="ваш_public_key"
export VAPID_PRIVATE_KEY="ваш_private_key"
export VAPID_EMAIL="mailto:your-email@example.com"

# Добавьте секреты в Supabase
supabase secrets set VAPID_PUBLIC_KEY="$VAPID_PUBLIC_KEY"
supabase secrets set VAPID_PRIVATE_KEY="$VAPID_PRIVATE_KEY"
supabase secrets set VAPID_EMAIL="$VAPID_EMAIL"
```

---

## Шаг 5: Деплой Edge Function

### 5.1. Убедитесь, что вы в правильной директории:
```bash
cd cosmos-of-hopes
```

### 5.2. Деплой функции:
```bash
supabase functions deploy send-like-notification
```

Вы должны увидеть что-то вроде:
```
Deploying function send-like-notification...
Function deployed successfully!
```

### 5.3. Проверка деплоя:

1. Откройте [Supabase Dashboard](https://app.supabase.com)
2. Перейдите в **Edge Functions**
3. Убедитесь, что функция `send-like-notification` появилась в списке

---

## Шаг 6: Проверка работы

### 6.1. Проверка через Dashboard:

1. Откройте **Edge Functions** → **send-like-notification**
2. Перейдите на вкладку **Logs**
3. Попробуйте поставить лайк на шар в приложении
4. Проверьте логи - должны появиться записи о вызове функции

### 6.2. Тестовая отправка (опционально):

Можно протестировать функцию вручную через Dashboard:
1. Откройте **Edge Functions** → **send-like-notification**
2. Нажмите **Invoke function**
3. Введите тестовые данные:
```json
{
  "toyOwnerId": "ваш_user_id",
  "toyId": "id_шара_для_теста"
}
```
4. Нажмите **Invoke**
5. Проверьте результат

---

## Шаг 7: Проверка в приложении

1. Откройте ваше приложение
2. Убедитесь, что у вас включены push-уведомления (кнопка 🔔 в правом верхнем углу)
3. Попросите другого пользователя (или откройте в другом браузере) поставить лайк на ваш шар
4. Вы должны получить push-уведомление: "✨ Ваш шар получил лайк!"

---

## Возможные проблемы и решения

### Проблема 1: "Function not found"
**Решение:** Убедитесь, что вы выполнили `supabase link` и указали правильный project-ref

### Проблема 2: "VAPID keys not configured"
**Решение:** Проверьте, что все три секрета (VAPID_PUBLIC_KEY, VAPID_PRIVATE_KEY, VAPID_EMAIL) добавлены в Supabase Secrets

### Проблема 3: "Unauthorized" при вызове функции
**Решение:** Проверьте, что в коде используется правильный `NEXT_PUBLIC_SUPABASE_ANON_KEY` для авторизации

### Проблема 4: Уведомления не приходят
**Проверьте:**
1. Разрешение на уведомления в браузере (должно быть "Разрешить")
2. Подписка сохранена в таблице `push_subscriptions` (проверьте в Supabase Dashboard → Table Editor)
3. VAPID ключи совпадают с теми, что в `.env.local` на клиенте
4. Логи Edge Function (может быть ошибка отправки)

### Проблема 5: Ошибка при деплое
**Решение:**
```bash
# Проверьте, что вы авторизованы
supabase login

# Проверьте связь с проектом
supabase projects list

# Попробуйте пересвязать проект
supabase link --project-ref ваш-project-ref
```

---

## Дополнительная информация

### Структура файлов:
```
cosmos-of-hopes/
  supabase/
    functions/
      send-like-notification/
        index.ts          # Код Edge Function
```

### Переменные окружения для Edge Function:
- `VAPID_PUBLIC_KEY` - публичный VAPID ключ
- `VAPID_PRIVATE_KEY` - приватный VAPID ключ (секретный!)
- `VAPID_EMAIL` - email для VAPID (формат: `mailto:email@example.com`)
- `SUPABASE_URL` - автоматически доступен
- `SUPABASE_SERVICE_ROLE_KEY` - автоматически доступен

### Логи и мониторинг:
- Логи Edge Function: **Supabase Dashboard** → **Edge Functions** → **send-like-notification** → **Logs**
- Логи приложения: Консоль браузера (F12) при добавлении лайка

---

## Готово! 🎉

После выполнения всех шагов уведомления о лайках будут работать автоматически:
- При добавлении лайка к шару
- Владелец шара получит push-уведомление (если подписан)
- Уведомление не отправляется, если лайк поставил сам владелец

