# 🔄 План корректного переподключения Jitsi сервера

## 📊 РЕЗЮМЕ: Текущее состояние

### ✅ Что уже готово в коде:

1. **Компонент VideoRoom.tsx** - полностью готов к работе
   - Использует простой и надежный iframe подход
   - Поддерживает переменную окружения `NEXT_PUBLIC_JITSI_SERVER_URL`
   - По умолчанию использует публичный сервер `https://meet.jit.si`
   - Код корректен, никаких ошибок нет

2. **Интеграция в приложение:**
   - Компонент используется в `app/room/page.tsx`
   - Работает как на ПК, так и на мобильных устройствах
   - Поддерживает полноэкранный режим
   - Показывает индикатор загрузки

3. **Зависимости:**
   - Пакет `@jitsi/react-sdk` установлен (но не используется - это нормально, мы используем iframe)
   - Никаких дополнительных зависимостей не требуется

### ❌ Что было проблемой ранее:

**ГЛАВНАЯ ПРОБЛЕМА:** Конфигурация nginx для Jitsi неправильно перехватывала запросы к основному домену `super2026.online` вместо того, чтобы работать только на `meet.super2026.online`.

**Результат:** Весь сайт перестал работать, открывался Jitsi вместо приложения.

**Решение тогда:** Экстренно отключили конфигурацию Jitsi в nginx.

---

## 🎯 ПЛАН ДЕЙСТВИЙ (3 варианта)

### ✅ ВАРИАНТ 1: Проверка и активация (РЕКОМЕНДУЕТСЯ)

**Когда использовать:** Если Jitsi уже установлен на сервере и просто отключен.

**Шаги:**

#### Шаг 1: Диагностика на сервере (5 минут)

Выполните на сервере через SSH:

```bash
# 1. Проверить установленные пакеты Jitsi
dpkg -l | grep jitsi

# 2. Проверить статус сервисов
systemctl status jitsi-videobridge2 --no-pager | head -15
systemctl status jicofo --no-pager | head -15
systemctl status prosody --no-pager | head -15

# 3. Проверить конфигурации nginx
ls -la /etc/nginx/sites-enabled/ | grep -i meet
ls -la /etc/nginx/sites-available/ | grep -i meet

# 4. Проверить конфигурационный файл Jitsi (если есть)
cat /etc/jitsi/meet/meet.super2026.online-config.js 2>/dev/null | head -30 || echo "Файл не найден"

# 5. Проверить переменную окружения
cd /root/cosmos-of-hopes
grep JITSI .env.local 2>/dev/null || echo "Переменная не найдена"

# 6. Проверить доступность Jitsi
curl -I https://meet.super2026.online 2>&1 | head -10
```

**Пришлите результаты этих команд.**

---

#### Шаг 2: Исправление конфигурации nginx (КРИТИЧЕСКИ ВАЖНО!)

**Проблема:** Конфигурация Jitsi должна работать ТОЛЬКО на `meet.super2026.online`, НЕ на `super2026.online`.

**Проверка текущей конфигурации:**

```bash
# Найти конфигурацию Jitsi
cat /etc/nginx/sites-available/*meet* 2>/dev/null
```

**Конфигурация должна содержать:**

```nginx
server {
    listen 80;
    listen [::]:80;
    server_name meet.super2026.online;  # ⚠️ ТОЛЬКО meet.super2026.online!
    
    # ... остальная конфигурация Jitsi ...
}
```

**ВАЖНО:** 
- ❌ НЕ должно быть `server_name super2026.online`
- ❌ НЕ должно быть `server_name *.super2026.online` (кроме meet)
- ❌ НЕ должно быть блоков `if`, которые перехватывают другие домены
- ✅ Должно быть ТОЛЬКО `server_name meet.super2026.online`

---

#### Шаг 3: Правильная настройка конфигурации Jitsi

**Если конфигурация Jitsi отключена (нет в sites-enabled):**

```bash
# 1. Проверить файл в sites-available
ls -la /etc/nginx/sites-available/ | grep -i meet

# 2. Убедиться, что server_name правильный
cat /etc/nginx/sites-available/meet.super2026.online.conf | grep server_name

# 3. Создать симлинк (если файл правильный)
ln -sf /etc/nginx/sites-available/meet.super2026.online.conf /etc/nginx/sites-enabled/zzz-meet.super2026.online.conf

# ⚠️ ВАЖНО: Префикс "zzz-" гарантирует, что конфигурация Jitsi загружается ПОСЛЕДНЕЙ
# Это предотвращает конфликты с основной конфигурацией
```

**Проверка порядка загрузки:**

```bash
ls -la /etc/nginx/sites-enabled/
```

Должно быть:
- `00-cosmos-of-hopes` (или подобное) - загружается первой
- `zzz-meet.super2026.online.conf` - загружается последней

---

#### Шаг 4: Проверка конфигурации nginx

```bash
# Проверить синтаксис
nginx -t

# Если ошибок нет - перезагрузить
systemctl reload nginx

# Проверить статус
systemctl status nginx
```

---

#### Шаг 5: Тест изоляции доменов

**Проверка основного домена (должен работать ваше приложение):**

```bash
curl -I -H "Host: super2026.online" http://localhost 2>&1 | head -5
```

**Должен вернуть:** HTTP/1.1 200 (ваше приложение)

**Проверка Jitsi домена (должен работать Jitsi):**

```bash
curl -I https://meet.super2026.online 2>&1 | head -5
```

**Должен вернуть:** HTTP/1.1 200 (Jitsi)

---

#### Шаг 6: Активация в приложении

**Если все проверки пройдены:**

```bash
cd /root/cosmos-of-hopes

# Добавить переменную окружения (если её нет)
echo "NEXT_PUBLIC_JITSI_SERVER_URL=https://meet.super2026.online" >> .env.local

# Или отредактировать файл
nano .env.local
# Добавить строку: NEXT_PUBLIC_JITSI_SERVER_URL=https://meet.super2026.online
```

**Перезапустить приложение:**

```bash
pm2 restart cosmos-of-hopes --update-env
```

---

### 🔄 ВАРИАНТ 2: Полная переустановка Jitsi

**Когда использовать:** Если Jitsi не установлен или установка полностью сломана.

**Время:** ~30-40 минут

Следуйте инструкции: `MD/JITSI_QUICK_START.md`

**Важные моменты:**
1. При установке указывайте домен БЕЗ кавычек: `meet.super2026.online`
2. После установки обязательно проверьте конфигурацию nginx (как в Варианте 1, Шаг 2)
3. Используйте префикс `zzz-` для конфигурации в sites-enabled

---

### 🚀 ВАРИАНТ 3: Использовать публичный сервер (быстро)

**Когда использовать:** Если нужна быстрая работа, свой сервер не критичен.

**Время:** ~2 минуты

**Действия:**

```bash
cd /root/cosmos-of-hopes

# Удалить или закомментировать переменную
nano .env.local
# Закомментировать: # NEXT_PUBLIC_JITSI_SERVER_URL=https://meet.super2026.online

# Перезапустить
pm2 restart cosmos-of-hopes --update-env
```

**Результат:** Приложение будет использовать `https://meet.jit.si` (публичный сервер).

**Плюсы:** Работает сразу, не требует настройки сервера
**Минусы:** Ограничения публичного сервера (лаги при большой нагрузке, возможные ограничения)

---

## 🔍 КРИТИЧЕСКИ ВАЖНО: Правильная конфигурация nginx

### ❌ НЕПРАВИЛЬНО (это сломало приложение):

```nginx
server {
    server_name super2026.online meet.super2026.online;  # ❌ НЕ ТАК!
    # ...
}
```

или

```nginx
server {
    server_name *.super2026.online;  # ❌ НЕ ТАК!
    # ...
}
```

### ✅ ПРАВИЛЬНО:

**Основная конфигурация (cosmos-of-hopes):**
```nginx
server {
    listen 80 default_server;
    server_name super2026.online www.super2026.online;
    
    location / {
        proxy_pass http://localhost:3000;
        # ...
    }
}
```

**Конфигурация Jitsi:**
```nginx
server {
    listen 80;
    server_name meet.super2026.online;  # ✅ ТОЛЬКО meet.super2026.online!
    
    # ... конфигурация Jitsi ...
}
```

---

## ✅ ЧЕКЛИСТ перед активацией

- [ ] Jitsi установлен и сервисы работают (`systemctl status jitsi-videobridge2`)
- [ ] Конфигурация nginx для Jitsi содержит ТОЛЬКО `server_name meet.super2026.online`
- [ ] Конфигурация Jitsi в sites-enabled имеет префикс `zzz-` (загружается последней)
- [ ] `nginx -t` не показывает ошибок
- [ ] Основной домен `super2026.online` работает (показывает приложение)
- [ ] Jitsi домен `meet.super2026.online` работает (показывает Jitsi)
- [ ] Переменная `NEXT_PUBLIC_JITSI_SERVER_URL` добавлена в `.env.local`
- [ ] Приложение перезапущено с `--update-env`

---

## 🎯 РЕКОМЕНДАЦИЯ

**Начните с ВАРИАНТА 1:**

1. Выполните диагностику (Шаг 1)
2. Пришлите результаты
3. Я помогу исправить конфигурацию nginx (Шаг 2)
4. После проверок активируем (Шаги 3-6)

**Это самый безопасный и правильный подход!**

---

## 📝 Примечания

- Код приложения НЕ требует изменений - он уже готов
- Пакет `@jitsi/react-sdk` можно оставить, но он не используется (используем iframe)
- Все изменения делаются на сервере (nginx конфигурация, переменные окружения)
- Публичный сервер `meet.jit.si` всегда доступен как резервный вариант

---

**Дата составления:** 2025-01-28
**Статус:** Готов к реализации

