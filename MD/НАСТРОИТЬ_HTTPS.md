# 🔒 Настройка HTTPS для super2026.online

## ⚠️ Важно
Настройка HTTPS **НЕ ПОМЕШАЕТ** вашей работе над регистрацией! Это просто настройка сервера, которая не изменяет код приложения.

## Время: 10-15 минут

## Шаг 1: Подключитесь к серверу

```bash
ssh root@5.129.223.23
```

## Шаг 2: Установите Certbot (если не установлен)

```bash
# Обновить список пакетов
apt update

# Установить Certbot и плагин для Nginx
apt install -y certbot python3-certbot-nginx
```

## Шаг 3: Проверьте, что домен указывает на сервер

Перед получением сертификата убедитесь, что домен правильно настроен:

```bash
# Проверить DNS
nslookup super2026.online
nslookup www.super2026.online
```

Оба должны вернуть IP: `5.129.223.23`

## Шаг 4: Убедитесь, что порты 80 и 443 открыты

```bash
# Проверить, что nginx слушает порты
netstat -tlnp | grep nginx
# или
ss -tlnp | grep nginx
```

Должны быть порты 80 и (после настройки) 443.

## Шаг 5: Получите SSL сертификат

```bash
# Получить бесплатный SSL сертификат от Let's Encrypt
certbot --nginx -d super2026.online -d www.super2026.online
```

Certbot спросит:
1. **Email** - введите ваш email (для уведомлений об истечении сертификата)
2. **Согласие с условиями** - введите `Y` и нажмите Enter
3. **Редирект HTTP → HTTPS** - выберите **2** (Redirect) - это значит, что все запросы по HTTP будут автоматически перенаправляться на HTTPS

⚠️ **Важно**: Выберите вариант "2 - Redirect", чтобы HTTP автоматически перенаправлялся на HTTPS.

## Шаг 6: Certbot автоматически обновит конфигурацию Nginx

Certbot сам:
- Получит сертификат
- Добавит конфигурацию для HTTPS (порт 443)
- Настроит редирект HTTP → HTTPS
- Настроит автоматическое обновление сертификата

## Шаг 7: Проверьте конфигурацию

```bash
# Проверить, что конфигурация правильная
nginx -t
```

Должно быть: `nginx: configuration file /etc/nginx/nginx.conf test is successful`

## Шаг 8: Перезагрузите Nginx (если нужно)

Certbot обычно сам перезагружает Nginx, но можно сделать вручную:

```bash
systemctl reload nginx
```

## Шаг 9: Проверьте работу HTTPS

```bash
# Проверить HTTPS
curl -I https://super2026.online

# Должен вернуть HTTP 200 (или другой успешный код)
```

## Шаг 10: Проверьте в браузере

1. Откройте в браузере: `https://super2026.online`
2. Должен появиться **зеленый замочек** 🔒 рядом с адресом
3. Сайт должен открываться по HTTPS
4. Если открыть `http://super2026.online` (без S) - должен автоматически редиректить на HTTPS

## ✅ Готово!

После этого:
- ✅ Сайт будет работать по HTTPS
- ✅ Service Worker сможет работать (требует HTTPS)
- ✅ Push уведомления смогут работать (требуют HTTPS)
- ✅ Код регистрации не изменится - всё будет работать как прежде

## Автоматическое обновление сертификата

Let's Encrypt сертификаты действительны 90 дней. Certbot автоматически настроил обновление.

Проверить, что автообновление работает:

```bash
systemctl status certbot.timer
```

Должно быть: `Active: active (waiting)`

## Если возникли проблемы

### Проблема: Certbot не может получить сертификат

**Возможные причины:**
1. Домен не указывает на этот сервер
2. Порты 80 или 443 закрыты в firewall
3. Nginx не работает

**Решение:**
```bash
# Проверить статус Nginx
systemctl status nginx

# Проверить логи Certbot
tail -50 /var/log/letsencrypt/letsencrypt.log

# Проверить, что домен доступен
curl -I http://super2026.online
```

### Проблема: После настройки HTTPS сайт не открывается

**Решение:**
```bash
# Посмотреть конфигурацию, которую создал Certbot
cat /etc/nginx/sites-available/cosmos-of-hopes

# Проверить конфигурацию
nginx -t

# Перезагрузить Nginx
systemctl reload nginx

# Проверить логи
tail -50 /var/log/nginx/error.log
```

### Проблема: Нужно изменить конфигурацию после Certbot

Certbot может добавить дополнительные настройки. Основная конфигурация должна остаться:

- **Порт 80** - редирект на HTTPS
- **Порт 443** - проксирование на `http://localhost:3000`

Если нужно что-то изменить - редактируйте файл:

```bash
nano /etc/nginx/sites-available/cosmos-of-hopes
```

Но **НЕ удаляйте** блоки с SSL сертификатами, которые добавил Certbot (они помечены комментариями `# managed by Certbot`).

## Проверка после настройки

Выполните эти команды, чтобы убедиться, что всё работает:

```bash
# 1. Проверить конфигурацию
nginx -t

# 2. Проверить статус Nginx
systemctl status nginx

# 3. Проверить HTTPS
curl -I https://super2026.online

# 4. Проверить редирект HTTP → HTTPS
curl -I http://super2026.online
# Должен вернуть: HTTP/1.1 301 Moved Permanently

# 5. Проверить сертификат
openssl s_client -connect super2026.online:443 -servername super2026.online < /dev/null 2>/dev/null | openssl x509 -noout -dates
```

## Что дальше?

После настройки HTTPS:
1. Уведомления начнут работать (Service Worker требует HTTPS)
2. Push уведомления смогут работать
3. Все функции приложения продолжат работать как прежде
4. Регистрация пользователей будет работать так же, как и раньше

