# 🚀 Установка собственного Jitsi сервера

## Текущая ситуация
- ✅ Сервер: Timeweb Cloud (IP: 5.129.223.23)
- ✅ Домен: super2026.online
- ⚠️ Jitsi использует публичный сервер meet.jit.si (тестовый режим)
- 🎯 **Цель:** Развернуть собственный Jitsi сервер для продакшена

---

## 📋 Требования

1. **Сервер:** Ubuntu 20.04 или 22.04 (рекомендуется 22.04)
2. **Домен:** Нужен поддомен для Jitsi (например: `meet.super2026.online` или `jitsi.super2026.online`)
3. **Порты:** Нужны открытые порты:
   - 80 (HTTP)
   - 443 (HTTPS)
   - 10000/UDP (RTP медиа)
   - 4443/TCP (Fallback для HTTPS)

---

## 🔧 Шаг 1: Подготовка сервера

### Подключитесь к серверу:
```bash
ssh root@5.129.223.23
```

### Обновите систему:
```bash
apt update && apt upgrade -y
```

### Установите необходимые пакеты:
```bash
apt install -y nginx certbot python3-certbot-nginx ufw
```

---

## 🌐 Шаг 2: Настройка DNS для поддомена

В панели управления доменом `super2026.online` добавьте **A-запись**:

```
Тип: A
Имя: meet  (или jitsi - на ваше усмотрение)
Значение: 5.129.223.23
TTL: 3600
```

**Результат:** Поддомен `meet.super2026.online` будет указывать на ваш сервер.

**Проверка (через 5-15 минут):**
```bash
nslookup meet.super2026.online
```
Должен вернуть IP: `5.129.223.23`

---

## 📦 Шаг 3: Установка Jitsi Meet

### Добавьте репозиторий Jitsi:
```bash
echo 'deb https://download.jitsi.org stable/' | sudo tee /etc/apt/sources.list.d/jitsi-stable.list
wget -qO - https://download.jitsi.org/jitsi-key.gpg.key | sudo apt-key add -
apt update
```

### Установите Jitsi Meet:
```bash
apt install -y jitsi-meet
```

**Во время установки:**
1. Введите домен для Jitsi: `meet.super2026.online`
2. Выберите: **"Generate a new self-signed certificate"** (временно, SSL настроим позже)

---

## 🔒 Шаг 4: Настройка SSL сертификата

### Генерация Let's Encrypt сертификата:
```bash
/usr/share/jitsi-meet/scripts/install-letsencrypt-cert.sh
```

**Введите домен:** `meet.super2026.online`

Это автоматически:
- Установит SSL сертификат
- Настроит автообновление
- Настроит Nginx для HTTPS

---

## 🔥 Шаг 5: Настройка файрвола

### Откройте необходимые порты:
```bash
ufw allow 80/tcp
ufw allow 443/tcp
ufw allow 10000/udp
ufw allow 4443/tcp
ufw allow OpenSSH
ufw enable
```

### Проверка статуса:
```bash
ufw status
```

---

## ⚙️ Шаг 6: Базовая настройка Jitsi

### Отключите публичные комнаты (опционально, для безопасности):
```bash
nano /etc/jitsi/meet/meet.super2026.online-config.js
```

Добавьте или измените:
```javascript
config.p2p = {
    enabled: true,
    stunServers: [
        { urls: 'stun:stun.l.google.com:19302' },
        { urls: 'stun:stun1.l.google.com:19302' }
    ]
};

config.enableWelcomePage = false;
config.enableNoisyMicDetection = true;
```

### Отключите водяные знаки (опционально):
```bash
nano /etc/jitsi/meet/meet.super2026.online-interface_config.js
```

Найдите и установите:
```javascript
SHOW_JITSI_WATERMARK: false,
SHOW_BRAND_WATERMARK: false,
SHOW_POWERED_BY: false,
```

### Перезапустите Jitsi:
```bash
systemctl restart jitsi-videobridge2
systemctl restart jicofo
systemctl restart prosody
systemctl reload nginx
```

---

## ✅ Шаг 7: Проверка работы

1. **Откройте в браузере:** `https://meet.super2026.online`
2. **Создайте тестовую комнату:** Введите любое имя комнаты (например: `test123`)
3. **Разрешите доступ к микрофону/камере**
4. **Проверьте работу видео/аудио**

Если всё работает - отлично! 🎉

---

## 📝 Шаг 8: Обновление кода приложения

После успешной установки нужно обновить код приложения, чтобы использовать ваш сервер вместо `meet.jit.si`.

**Файл для изменения:** `components/rooms/VideoRoom.tsx`

**Изменение:**
- Заменить `https://meet.jit.si/` на `https://meet.super2026.online/`

---

## 🎛️ Дополнительная настройка (опционально)

### Ограничение участников (если нужно):
```bash
nano /etc/jitsi/jicofo/sip-communicator.properties
```

Добавьте:
```
org.jitsi.jicofo.max.participants=50
```

### Запись конференций (требует дополнительной настройки):
Нужно установить Jibri - отдельный сервис для записи.

---

## 🔍 Решение проблем

### Jitsi не запускается:
```bash
systemctl status jitsi-videobridge2
systemctl status jicofo
systemctl status prosody
journalctl -u jitsi-videobridge2 -n 50
```

### Проблемы с SSL:
```bash
certbot certificates
certbot renew --dry-run
```

### Проблемы с портами:
```bash
netstat -tulpn | grep -E ':(80|443|10000|4443)'
```

### Логи:
```bash
tail -f /var/log/jitsi/jicofo.log
tail -f /var/log/jitsi/jvb.log
```

---

## 📚 Полезные команды

```bash
# Перезапуск всех сервисов Jitsi
systemctl restart jitsi-videobridge2 jicofo prosody nginx

# Проверка статуса
systemctl status jitsi-videobridge2
systemctl status jicofo
systemctl status prosody

# Обновление Jitsi
apt update && apt upgrade jitsi-meet

# Просмотр активных конференций
prosodyctl mod_stats
```

---

## 🎯 Что дальше?

После установки:
1. ✅ Проверьте работу: `https://meet.super2026.online`
2. ✅ Обновите код приложения (следующий шаг)
3. ✅ Протестируйте видеокомнаты в вашем приложении
4. ✅ Настройте мониторинг (опционально)

---

## ⚠️ Важные замечания

1. **Резервное копирование:** Рекомендуется сделать бэкап конфигурации перед изменениями
2. **Производительность:** Jitsi требует ресурсов CPU и памяти, особенно при большом количестве участников
3. **TURN сервер:** Для сложных NAT может понадобиться дополнительный TURN сервер
4. **Безопасность:** Рекомендуется настроить аутентификацию для приватных комнат (требует дополнительной настройки)

---

**Дата создания:** 2025-01-28  
**Версия Jitsi:** Последняя стабильная из официального репозитория

