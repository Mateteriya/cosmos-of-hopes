# ✅ Push-уведомления: Готово к настройке!

## 📦 Что создано

1. **Node.js сервер** (`scripts/send-push-notifications-server.js`)
   - ✅ Проверяет уведомления каждые 5 минут
   - ✅ Отправляет в 23:57 (шар в космос)
   - ✅ Отправляет в 22:50 (запуск комнаты)
   - ✅ Учитывает timezone пользователя
   - ✅ Автоматически очищает недействительные подписки

2. **Edge Function** (`supabase/functions/send-new-year-notifications/index.ts`)
   - Альтернативный вариант через Supabase
   - Требует дополнительной настройки

3. **Инструкции**
   - `PUSH_NOTIFICATIONS_QUICK_START.md` - быстрый старт
   - `PUSH_NOTIFICATIONS_SETUP_GUIDE.md` - полная настройка

## 🚀 Следующие шаги (5 минут)

### 1. Установить зависимости:

```bash
npm install
```

### 2. Сгенерировать VAPID ключи:

```bash
npx web-push generate-vapid-keys
```

### 3. Добавить в `.env.local`:

```env
VAPID_PUBLIC_KEY=ваш_public_key_из_команды_выше
VAPID_PRIVATE_KEY=ваш_private_key_из_команды_выше
VAPID_EMAIL=mailto:your-email@example.com
NEXT_PUBLIC_VAPID_PUBLIC_KEY=ваш_public_key_из_команды_выше
SUPABASE_SERVICE_ROLE_KEY=ваш_service_role_key_из_supabase
```

### 4. Запустить сервер на вашем сервере:

```bash
# На сервере
cd /path/to/cosmos-of-hopes
npm install
pm2 start scripts/send-push-notifications-server.js --name push-notifications
pm2 save
pm2 startup
```

## ✅ Готово!

Сервер будет работать постоянно и отправлять уведомления в нужное время.

## 📝 Проверка работы

Логи покажут:
- Количество проверенных подписок
- Количество отправленных уведомлений
- Ошибки (если есть)

## 💡 Важно

- Сервер должен работать постоянно (через PM2)
- Проверка каждые 5 минут
- Уведомления только 31 декабря в нужное время
- Для теста можно временно изменить время в коде

