# 🔍 Диагностика проблемы с кешем

## Проблема
Изменения применены на сервере, но на сайте ничего не изменилось.

## Решения (по порядку):

### 1. Очистить кеш браузера

**В Chrome/Edge:**
1. Нажмите `Ctrl + Shift + Delete`
2. Выберите "Изображения и файлы в кеше"
3. Выберите "За все время"
4. Нажмите "Удалить данные"

**Или вручную:**
1. Нажмите `F12` (открыть DevTools)
2. Правой кнопкой на кнопку обновления страницы (↻)
3. Выберите "Очистить кеш и жесткая перезагрузка" (Empty Cache and Hard Reload)

### 2. Проверить Service Worker

Service Worker может кешировать старую версию:

1. Откройте DevTools (`F12`)
2. Перейдите на вкладку **Application** (Приложение)
3. В левом меню выберите **Service Workers**
4. Найдите Service Worker для `super2026.online`
5. Нажмите кнопку **Unregister** (отменить регистрацию)
6. Обновите страницу (`Ctrl + F5`)

### 3. Проверить консоль браузера

Откройте консоль (`F12` → `Console`) и проверьте:

1. Есть ли ошибки (красным цветом)?
2. Есть ли сообщения начинающиеся с `[NotificationPromptButton]`?
3. Если есть ошибки - скопируйте их

### 4. Проверить, что компонент рендерится

В консоли браузера выполните:

```javascript
// Проверить, загружен ли компонент
console.log('Checking NotificationPromptButton...');

// Проверить Service Worker
navigator.serviceWorker.getRegistrations().then(regs => {
  console.log('Service Workers:', regs.length);
  regs.forEach(reg => console.log('SW:', reg.scope, reg.active));
});
```

### 5. Принудительная перезагрузка

Попробуйте:
- `Ctrl + F5` (жесткая перезагрузка)
- Или `Ctrl + Shift + R`

### 6. Проверить в режиме инкогнито

Откройте сайт в режиме инкогнито (Ctrl+Shift+N) - там нет кеша.

### 7. Если ничего не помогло - пересобрать с очисткой кеша Next.js

На сервере:

```bash
cd /root/cosmos-of-hopes

# Удалить папку .next (кеш Next.js)
rm -rf .next

# Пересобрать
npm run build

# Перезапустить
pm2 restart cosmos-of-hopes --update-env
```

## Что проверить в консоли браузера

После открытия сайта откройте консоль (F12 → Console) и найдите сообщения:

- `[NotificationPromptButton] Initializing...` - компонент начал работу
- `[NotificationPromptButton] Push notifications supported` - поддерживаются
- `[NotificationPromptButton] Service Worker registered` - Service Worker зарегистрирован
- `[NotificationPromptButton] Subscription status: false` - подписки нет (должна показаться кнопка)
- `[NotificationPromptButton] Subscription status: true` - подписка есть (кнопка не покажется)

**Пришлите, что выводится в консоли!**

