# 🔧 Исправление проблем с Яндекс браузером

## Проблема
- В Chrome приложение работает идеально
- В Яндекс браузере загружается медленно или не загружается вообще
- Ошибка "Не удаётся установить соединение с сайтом"

## Причины

### 1. Service Worker блокирует загрузку
Яндекс браузер может по-другому обрабатывать регистрацию Service Worker, что может блокировать загрузку страницы.

### 2. Таймауты при регистрации
Если Service Worker не может загрузиться (проблемы с сетью, SSL и т.д.), это может блокировать загрузку.

### 3. Агрессивное кеширование
Яндекс браузер может кешировать старые версии Service Worker.

## Решения

### ✅ Что уже исправлено:

1. **Таймаут для регистрации Service Worker**
   - Добавлен таймаут 3 секунды
   - Если Service Worker не зарегистрировался за это время, приложение продолжает работать

2. **Отложенная регистрация**
   - Service Worker регистрируется через 1 секунду после загрузки страницы
   - Не блокирует первоначальную загрузку

3. **Обработка ошибок**
   - Все ошибки регистрации обрабатываются gracefully
   - Приложение работает даже если Service Worker недоступен

## Что проверить на сервере:

### 1. Проверьте доступность Service Worker:
```bash
curl -I https://super2026.online/sw.js
```

Должен вернуть:
```
HTTP/2 200
Content-Type: application/javascript; charset=utf-8
```

### 2. Проверьте логи PM2:
```bash
pm2 logs cosmos-of-hopes --lines 50
```

### 3. Проверьте использование памяти:
```bash
pm2 monit
```

Если память заканчивается - это может быть причиной медленной загрузки.

### 4. Проверьте SSL сертификат:
```bash
curl -vI https://super2026.online
```

### 5. Очистите кеш Next.js:
```bash
cd ~/cosmos-of-hopes
rm -rf .next
npm run build
pm2 restart cosmos-of-hopes
```

## Дополнительные рекомендации:

### Для пользователей Яндекс браузера:

1. **Очистить кеш браузера:**
   - Настройки → Дополнительно → Очистить данные
   - Выбрать "Кешированные изображения и файлы"

2. **Отключить расширения:**
   - Некоторые расширения могут блокировать Service Worker

3. **Проверить настройки безопасности:**
   - Убедиться, что сайт не заблокирован

### Если проблема сохраняется:

1. **Проверьте логи сервера:**
   ```bash
   tail -f /var/log/nginx/error.log
   ```

2. **Проверьте доступность сервера:**
   ```bash
   ping super2026.online
   ```

3. **Проверьте DNS:**
   ```bash
   nslookup super2026.online
   ```

## Мониторинг:

После обновления проверьте:
- [ ] Chrome работает нормально
- [ ] Яндекс браузер загружается быстрее
- [ ] Service Worker регистрируется (F12 → Application → Service Workers)
- [ ] Нет ошибок в консоли браузера

