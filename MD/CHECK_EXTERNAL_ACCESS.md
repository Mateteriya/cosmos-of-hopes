# 🔍 Проверка внешнего доступа к приложению

## Проблема
Приложение работает локально (curl показывает HTTP 200), но не открывается из браузера.

## Диагностика

### Шаг 1: Проверить, что приложение доступно извне

```bash
# Проверить, слушает ли nginx на всех интерфейсах
netstat -tlnp | grep :80

# Должно быть что-то вроде:
# tcp  0  0 0.0.0.0:80  ... nginx
# или
# tcp  0  0 :::80  ... nginx
```

### Шаг 2: Проверить файрвол

```bash
# Проверить статус ufw (если используется)
ufw status

# Если порт 80 закрыт, открыть:
ufw allow 80/tcp
ufw reload

# Проверить iptables
iptables -L -n | grep 80
```

### Шаг 3: Проверить DNS

```bash
# Проверить, что домен указывает на правильный IP
nslookup super2026.online
dig super2026.online

# Должен вернуть IP: 5.129.223.23
```

### Шаг 4: Проверить доступность с внешнего IP

```bash
# Попробовать подключиться с сервера к самому себе по внешнему IP
curl -I -H "Host: super2026.online" http://5.129.223.23

# Или через внешний сервис (если есть доступ)
# curl -I http://super2026.online
```

### Шаг 5: Проверить логи nginx на наличие внешних запросов

```bash
# Очистить логи
echo "" > /var/log/nginx/access.log

# Попробовать открыть в браузере http://super2026.online
# Затем проверить логи
tail -20 /var/log/nginx/access.log

# Если запросов нет - проблема в DNS или файрволе
# Если запросы есть, но ошибки - проблема в конфигурации
```

### Шаг 6: Проверить, что nginx слушает на всех интерфейсах

```bash
# Проверить конфигурацию nginx
nginx -T 2>/dev/null | grep "listen 80"

# Должно быть: listen 80 default_server;
# Без указания конкретного IP (0.0.0.0 или ::)
```

### Шаг 7: Проверить конфигурацию cosmos-of-hopes

```bash
cat /etc/nginx/sites-available/cosmos-of-hopes
```

Убедитесь, что:
- `listen 80 default_server;` (без указания IP)
- Нет ограничений по IP

## Возможные решения

### Решение 1: Открыть порт 80 в файрволе

```bash
# Если используется ufw
ufw allow 80/tcp
ufw allow 443/tcp
ufw reload

# Если используется iptables напрямую
iptables -A INPUT -p tcp --dport 80 -j ACCEPT
iptables -A INPUT -p tcp --dport 443 -j ACCEPT
```

### Решение 2: Проверить настройки провайдера/хостинга

Возможно, порт 80 заблокирован на уровне провайдера. Проверьте панель управления хостингом.

### Решение 3: Проверить DNS

Убедитесь, что DNS записи настроены правильно:
- A запись: `super2026.online` → `5.129.223.23`
- A запись: `www.super2026.online` → `5.129.223.23`

### Решение 4: Временное решение - использовать другой порт

Если порт 80 заблокирован, можно временно использовать другой порт:

```bash
# Изменить конфигурацию
nano /etc/nginx/sites-available/cosmos-of-hopes
```

Изменить:
```nginx
listen 80 default_server;
```

На:
```nginx
listen 8080 default_server;
```

И открыть порт 8080 в файрволе.

## Диагностика (выполните и пришлите вывод)

```bash
echo "=== Порты nginx ==="
netstat -tlnp | grep nginx

echo "=== Статус файрвола ==="
ufw status 2>/dev/null || echo "ufw не установлен"
iptables -L -n | grep -E "80|443" | head -10

echo "=== DNS проверка ==="
nslookup super2026.online
dig +short super2026.online

echo "=== Тест с внешнего IP ==="
curl -I -H "Host: super2026.online" http://5.129.223.23 2>&1 | head -20

echo "=== Логи nginx (последние 10 строк) ==="
tail -10 /var/log/nginx/access.log
```

