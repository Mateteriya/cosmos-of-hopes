# 🔔 Исправление уведомлений в продакшн - Краткая инструкция

## Проблема
Уведомления работают локально, но не работают на продакшн сайте.

## Быстрое решение (3 шага)

### Шаг 1: Проверьте переменные окружения на сервере

Подключитесь к серверу:
```bash
ssh root@5.129.223.23
```

Перейдите в папку проекта:
```bash
cd /root/cosmos-of-hopes
```

Проверьте файл `.env.local`:
```bash
nano .env.local
```

**Убедитесь, что в файле есть эти строки:**
```env
NEXT_PUBLIC_VAPID_PUBLIC_KEY=ваш_public_ключ
VAPID_PRIVATE_KEY=ваш_private_ключ
NEXT_PUBLIC_SUPABASE_URL=ваш_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=ваш_anon_key
SUPABASE_SERVICE_ROLE_KEY=ваш_service_role_key
```

⚠️ **Если ключей VAPID нет** - сгенерируйте их на ЛОКАЛЬНОЙ машине:

```bash
# На локальной машине (не на сервере)
cd cosmos-of-hopes
npm install web-push
npm run generate-vapid-keys
```

Скопируйте оба ключа и добавьте их в `.env.local` на сервере.

### Шаг 2: Пересоберите и перезапустите приложение на сервере

На сервере выполните:
```bash
cd /root/cosmos-of-hopes

# Установите зависимости (если нужно)
npm install

# Пересоберите приложение
npm run build

# Перезапустите приложение
pm2 restart cosmos-of-hopes --update-env
```

### Шаг 3: Проверьте работу

1. Откройте ваш сайт в браузере: `https://super2026.online`
2. Нажмите кнопку "Включить уведомления" (правый верхний угол)
3. Разрешите уведомления в браузере
4. Должно появиться тестовое уведомление

## Проверка в браузере

Откройте DevTools (F12) и проверьте:

1. **Вкладка Console** - не должно быть ошибок о VAPID ключах
2. **Вкладка Application → Service Workers** - должен быть зарегистрирован Service Worker
3. Откройте в браузере: `https://super2026.online/sw.js` - должен открыться JavaScript файл

## Если не помогло

Проверьте, что сайт работает по HTTPS (не HTTP):
- Service Worker требует HTTPS
- Push уведомления требуют HTTPS

Если всё ещё не работает - проверьте полную инструкцию в файле `FIX_NOTIFICATIONS_PRODUCTION.md`

