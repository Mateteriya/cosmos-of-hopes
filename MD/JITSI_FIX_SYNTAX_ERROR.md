# 🔧 Исправление синтаксической ошибки

## Проблема
```
SyntaxError: Unexpected token '{'
at line 132
```

## Решение: Проверить контекст вокруг строки 132

Выполните на сервере:

```bash
cd /etc/jitsi/meet
sed -n '125,140p' meet.super2026.online-config.js
```

Это покажет строки 125-140, чтобы увидеть, что не так.

---

## Вероятная причина

Скорее всего, отсутствует запятая после предыдущего свойства перед `iceServers`.

---

## Быстрое исправление через sed

Если проблема в отсутствии запятой, выполните:

```bash
# Восстановить из бэкапа
cp meet.super2026.online-config.js.backup4 meet.super2026.online-config.js

# Или если бэкапа нет - восстановить из предыдущего
cp meet.super2026.online-config.js.backup3 meet.super2026.online-config.js
```

Затем попробуйте снова, но **обязательно добавьте запятую** после `disableReactions: true,`

---

**Дата:** 2025-12-31

