# 🚨 СРОЧНО: Запуск процесса уведомлений на сервере

## Шаг 1: Проверьте зависимости

```bash
cd /root/cosmos-of-hopes

# Проверьте, установлены ли зависимости
npm list web-push node-cron @supabase/supabase-js

# Если нет - установите:
npm install web-push node-cron @supabase/supabase-js dotenv
```

## Шаг 2: Проверьте переменные окружения

```bash
# Проверьте файл .env.local
cat .env.local | grep -E "VAPID|SUPABASE"

# Должны быть:
# - VAPID_PUBLIC_KEY
# - VAPID_PRIVATE_KEY
# - VAPID_EMAIL (или будет использован дефолтный)
# - NEXT_PUBLIC_SUPABASE_URL
# - SUPABASE_SERVICE_ROLE_KEY
```

## Шаг 3: Запустите процесс через PM2

```bash
cd /root/cosmos-of-hopes

# Запустите процесс
pm2 start scripts/send-push-notifications-server.js --name push-notifications --update-env

# Сохраните конфигурацию PM2
pm2 save

# Проверьте статус
pm2 status

# Проверьте логи
pm2 logs push-notifications --lines 50
```

## Шаг 4: Проверьте работу

Логи должны показывать:
- ✅ "Проверка уведомлений запущена"
- ✅ "Проверка времени для каждого пользователя..."
- ✅ Нет ошибок о VAPID ключах

## Если есть ошибки:

### Ошибка: "VAPID ключи не настроены"
→ Проверьте `.env.local` на сервере, добавьте ключи

### Ошибка: "Cannot find module"
→ Выполните: `npm install` в папке проекта

### Ошибка: "Supabase ключи не настроены"
→ Проверьте `SUPABASE_SERVICE_ROLE_KEY` в `.env.local`

