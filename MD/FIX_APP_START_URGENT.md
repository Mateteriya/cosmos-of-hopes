# 🚨 СРОЧНО: Запуск приложения на порту 3000

## Проблема
Конфигурация nginx правильная, но приложение не отвечает на порту 3000.

## Проверка и исправление

### Шаг 1: Проверить, запущено ли приложение

```bash
# Проверить процессы на порту 3000
netstat -tlnp | grep 3000
# или
ss -tlnp | grep 3000
# или
lsof -i :3000
```

Если ничего не выводится - приложение НЕ запущено.

### Шаг 2: Перейти в директорию проекта

```bash
# Найти директорию проекта
find /root -name "cosmos-of-hopes" -type d 2>/dev/null
# или
find /home -name "cosmos-of-hopes" -type d 2>/dev/null
# или
ls -la /var/www/
```

Когда найдете директорию, перейдите в неё:
```bash
cd /путь/к/cosmos-of-hopes
```

### Шаг 3: Проверить, что Next.js установлен

```bash
# Проверить package.json
cat package.json | grep -A 5 "scripts"

# Если node_modules отсутствует, установить зависимости
npm install
```

### Шаг 4: Собрать и запустить приложение

```bash
# Собрать приложение для продакшн
npm run build

# Запустить приложение
npm run start
```

**ИЛИ** если используете PM2:

```bash
# Установить PM2 (если не установлен)
npm install -g pm2

# Запустить приложение через PM2
pm2 start npm --name "cosmos-of-hopes" -- start

# Или если уже есть процесс, перезапустить
pm2 restart cosmos-of-hopes

# Проверить статус
pm2 status
pm2 logs cosmos-of-hopes
```

### Шаг 5: Проверить, что приложение отвечает

```bash
# Проверить локально на сервере
curl http://localhost:3000

# Должен вернуть HTML код главной страницы
```

### Шаг 6: Перезагрузить nginx

```bash
systemctl reload nginx
```

### Шаг 7: Проверить работу домена

Откройте в браузере: `http://super2026.online`

Должна открыться главная страница вашего приложения.

## Если приложение не запускается

### Проверить ошибки:

```bash
# Если запускаете через npm
npm run start 2>&1 | tee /tmp/app-errors.log

# Если через PM2
pm2 logs cosmos-of-hopes --lines 50
```

### Проверить переменные окружения:

```bash
# Проверить .env файлы
ls -la .env*
cat .env.local 2>/dev/null || echo "Файл .env.local не найден"
cat .env 2>/dev/null || echo "Файл .env не найден"
```

### Проверить порт:

```bash
# Убедиться, что порт 3000 свободен
lsof -i :3000

# Если порт занят другим процессом, убить его
kill -9 $(lsof -t -i:3000)
```

## Автозапуск приложения (PM2)

Чтобы приложение запускалось автоматически после перезагрузки сервера:

```bash
# Сохранить текущие процессы PM2
pm2 save

# Настроить автозапуск
pm2 startup
# Выполните команду, которую выведет PM2
```

## Проверка после исправления

1. Приложение должно быть доступно на `http://localhost:3000` (локально на сервере)
2. Домен `http://super2026.online` должен открывать ваше приложение
3. Домен `https://meet.super2026.online` должен открывать Jitsi

## Диагностика (если проблема осталась)

Выполните и пришлите вывод:

```bash
echo "=== Процессы на порту 3000 ==="
netstat -tlnp | grep 3000

echo "=== Статус PM2 ==="
pm2 status 2>/dev/null || echo "PM2 не установлен"

echo "=== Процессы Node.js ==="
ps aux | grep node

echo "=== Содержимое директории проекта ==="
pwd
ls -la

echo "=== package.json scripts ==="
cat package.json | grep -A 10 "scripts" 2>/dev/null || echo "package.json не найден"
```

