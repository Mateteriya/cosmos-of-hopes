# 🔄 Обновление кода для использования собственного Jitsi сервера

## ✅ Что уже сделано

Код компонента `VideoRoom.tsx` обновлен для поддержки переменной окружения `NEXT_PUBLIC_JITSI_SERVER_URL`.

**По умолчанию** используется публичный сервер `https://meet.jit.si` (для обратной совместимости).

---

## 📝 Шаг 1: Добавить переменную окружения

### На локальной машине (для разработки):

1. Откройте файл `.env.local` в папке `cosmos-of-hopes/`
2. Добавьте строку:
```env
NEXT_PUBLIC_JITSI_SERVER_URL=https://meet.super2026.online
```

**Если файла `.env.local` нет**, создайте его и добавьте:
```env
NEXT_PUBLIC_JITSI_SERVER_URL=https://meet.super2026.online
NEXT_PUBLIC_SUPABASE_URL=ваш_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=ваш_supabase_anon_key
NEXT_PUBLIC_APP_URL=https://super2026.online
```

3. Перезапустите dev сервер:
```bash
npm run dev
```

---

## 🚀 Шаг 2: Настроить на продакшн сервере

### Вариант A: Переменная окружения (рекомендуется)

На сервере создайте или обновите файл `.env.local`:

```bash
ssh root@5.129.223.23
cd /root/cosmos-of-hopes
nano .env.local
```

Добавьте:
```env
NEXT_PUBLIC_JITSI_SERVER_URL=https://meet.super2026.online
```

Сохраните (Ctrl+O, Enter, Ctrl+X).

### Вариант B: Через PM2 (если используете PM2)

```bash
pm2 restart cosmos-of-hopes --update-env --env production
```

Или установите переменную через PM2:
```bash
pm2 set NEXT_PUBLIC_JITSI_SERVER_URL https://meet.super2026.online
pm2 restart cosmos-of-hopes
```

---

## 🔧 Шаг 3: Пересобрать приложение

После добавления переменной окружения на сервере:

```bash
cd /root/cosmos-of-hopes
git pull origin main
npm run build
pm2 restart cosmos-of-hopes --update-env
```

Или если не используете PM2:
```bash
cd /root/cosmos-of-hopes
git pull origin main
npm run build
npm run start
```

---

## ✅ Шаг 4: Проверка работы

1. Откройте ваше приложение: `https://super2026.online`
2. Перейдите в любую комнату
3. Откройте видеокомнату
4. Проверьте, что URL в iframe указывает на ваш сервер: `https://meet.super2026.online/...`

**Как проверить URL:**
- Откройте консоль разработчика (F12)
- Перейдите на вкладку "Network"
- Найдите запросы к `meet.super2026.online`
- Или в консоли выполните: `document.querySelector('iframe').src`

---

## 🔄 Возврат к публичному серверу (если нужно)

Если нужно временно вернуться к публичному серверу:

1. Удалите или закомментируйте строку в `.env.local`:
```env
# NEXT_PUBLIC_JITSI_SERVER_URL=https://meet.super2026.online
```

2. Пересоберите и перезапустите приложение

Или установите явно:
```env
NEXT_PUBLIC_JITSI_SERVER_URL=https://meet.jit.si
```

---

## 📋 Чеклист перед продакшн деплоем

- [ ] Jitsi сервер установлен и работает на `https://meet.super2026.online`
- [ ] SSL сертификат установлен и валиден
- [ ] Переменная `NEXT_PUBLIC_JITSI_SERVER_URL` добавлена в `.env.local` на сервере
- [ ] Приложение пересобрано (`npm run build`)
- [ ] Приложение перезапущено
- [ ] Проверена работа видеокомнаты в браузере
- [ ] Проверено, что URL указывает на ваш сервер

---

## ⚠️ Важные замечания

1. **Имя переменной:** Должно начинаться с `NEXT_PUBLIC_`, чтобы быть доступной в клиентском коде
2. **Формат URL:** Не добавляйте слеш в конце: `https://meet.super2026.online` (не `https://meet.super2026.online/`)
3. **HTTPS обязательно:** Jitsi требует HTTPS для работы микрофона и камеры
4. **Кэш браузера:** После изменения может потребоваться очистить кэш браузера (Ctrl+Shift+R)

---

## 🐛 Решение проблем

### Видеокомната не загружается

1. Проверьте, что Jitsi сервер доступен:
```bash
curl https://meet.super2026.online
```

2. Проверьте переменную окружения:
```bash
# На сервере
cd /root/cosmos-of-hopes
cat .env.local | grep JITSI
```

3. Проверьте логи:
```bash
pm2 logs cosmos-of-hopes
# или
tail -f /var/log/nginx/error.log
```

### Все еще используется старый сервер

1. Очистите кэш браузера (Ctrl+Shift+R или Ctrl+F5)
2. Убедитесь, что приложение пересобрано:
```bash
npm run build
```
3. Проверьте, что переменная окружения установлена корректно

---

**Дата создания:** 2025-01-28

