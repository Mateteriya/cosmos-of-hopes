# 🔧 Пошаговая настройка сервера Jitsi для исправления проблем

## ⚠️ ВАЖНО: Выполняйте команды ПО ОТДЕЛЬНОСТИ, не склеивайте их!

---

## Шаг 1: Подключение к серверу и переход в нужную директорию

```bash
# Подключитесь к серверу (если еще не подключены)
ssh root@ваш_сервер

# Перейдите в директорию с конфигурацией Jitsi
cd /etc/jitsi/meet/
```

---

## Шаг 2: Проверка существующих файлов конфигурации

```bash
# Проверьте, какие файлы есть
ls -la
```

**Должны быть файлы:**
- `meet.super2026.online-config.js`
- `meet.super2026.online-interface_config.js`

Если файлов нет - сообщите, и я дам команды для их создания.

---

## Шаг 3: Бэкап существующих конфигураций (ОБЯЗАТЕЛЬНО!)

```bash
# Создаем резервные копии
cp meet.super2026.online-config.js meet.super2026.online-config.js.backup
cp meet.super2026.online-interface_config.js meet.super2026.online-interface_config.js.backup

# Проверяем, что бэкапы созданы
ls -la *.backup
```

---

## Шаг 4: Редактирование основного конфига (config.js)

```bash
# Открываем файл для редактирования
nano meet.super2026.online-config.js
```

**Найдите строку с `var config = {` и добавьте/измените следующие настройки:**

```javascript
var config = {
    // ... существующие настройки (НЕ УДАЛЯЙТЕ ИХ!) ...
    
    // Отключение P2P для стабильности (ДОБАВИТЬ ЭТО)
    p2p: {
        enabled: false,
        stunServers: [
            { urls: 'stun:stun.l.google.com:19302' },
            { urls: 'stun:stun1.l.google.com:19302' }
        ]
    },
    
    // Настройки ICE для стабильного соединения (ДОБАВИТЬ ЭТО)
    iceTransportPolicy: 'all',
    iceServers: [
        {
            urls: 'stun:stun.l.google.com:19302'
        },
        {
            urls: 'stun:stun1.l.google.com:19302'
        }
    ],
    
    // Отключение плейсхолдера (ДОБАВИТЬ ЭТО)
    prejoinPageEnabled: false,
    enableWelcomePage: false,
    
    // Настройки аудио (ДОБАВИТЬ ЭТО)
    enableNoAudioDetection: true,
    enableNoisyMicDetection: true,
    audioLevelsInterval: 200,
    
    // Настройки видео (ДОБАВИТЬ ЭТО)
    channelLastN: 10,
    startWithVideoMuted: false,
    startWithAudioMuted: false,
    
    // Отключение глубоких ссылок (ДОБАВИТЬ ЭТО)
    disableDeepLinking: true,
    disableInviteFunctions: true,
    disableThirdPartyRequests: true,
};
```

**Как редактировать в nano:**
1. Используйте стрелки для навигации
2. Найдите нужное место (обычно в конце объекта `config`)
3. Добавьте новые строки
4. Нажмите `Ctrl+O` для сохранения
5. Нажмите `Enter` для подтверждения
6. Нажмите `Ctrl+X` для выхода

---

## Шаг 5: Редактирование интерфейсного конфига (interface_config.js)

```bash
# Открываем файл для редактирования
nano meet.super2026.online-interface_config.js
```

**Найдите строку с `var interfaceConfig = {` и добавьте/измените:**

```javascript
var interfaceConfig = {
    // ... существующие настройки (НЕ УДАЛЯЙТЕ ИХ!) ...
    
    // Отключение реакций (эмодзи) - ДОБАВИТЬ ЭТО
    DISABLE_REACTIONS: true,
    DISABLE_JOIN_LEAVE_NOTIFICATIONS: true,
    DISABLE_PRESENCE_STATUS: true,
    
    // Убираем кнопку "поднять руку" из тулбара - ИЗМЕНИТЬ ЭТО
    TOOLBAR_BUTTONS: [
        'microphone', 'camera', 'closedcaptions', 'desktop',
        'fullscreen', 'fodeviceselection', 'hangup', 'profile',
        'chat', 'recording', 'livestreaming', 'settings',
        'videoquality', 'filmstrip', 'invite', 'feedback',
        'stats', 'shortcuts', 'tileview', 'videobackgroundblur',
        'download', 'help', 'mute-everyone', 'security'
        // УБРАЛИ: 'raisehand' - вызывает панель с эмодзи
    ],
};
```

**Важно:** Если в `TOOLBAR_BUTTONS` есть `'raisehand'` - удалите его!

---

## Шаг 6: Проверка синтаксиса файлов

```bash
# Проверяем синтаксис JavaScript (если установлен node)
node -c meet.super2026.online-config.js
node -c meet.super2026.online-interface_config.js
```

Если ошибок нет - продолжаем. Если есть ошибки - исправьте их в nano.

---

## Шаг 7: Перезапуск сервисов Jitsi

```bash
# Перезапускаем Videobridge (видео-сервер)
systemctl restart jitsi-videobridge2

# Проверяем статус
systemctl status jitsi-videobridge2
```

**Если статус "active (running)" - продолжаем. Если ошибка - сообщите.**

```bash
# Перезапускаем Prosody (XMPP сервер)
systemctl restart prosody

# Проверяем статус
systemctl status prosody
```

```bash
# Перезапускаем Jicofo (координатор)
systemctl restart jicofo

# Проверяем статус
systemctl status jicofo
```

```bash
# Перезагружаем Nginx
systemctl reload nginx

# Проверяем статус
systemctl status nginx
```

---

## Шаг 8: Проверка логов (если что-то не работает)

```bash
# Логи Videobridge
tail -n 50 /var/log/jitsi/jvb.log

# Логи Jicofo
tail -n 50 /var/log/jitsi/jicofo.log

# Логи Prosody
tail -n 50 /var/log/prosody/prosody.log
```

**Ищите ошибки (ERROR, FATAL, Exception). Если есть - сообщите.**

---

## Шаг 9: Тестирование

1. Откройте комнату на мобильном и ПК
2. Проверьте:
   - ✅ Плейсхолдер не появляется
   - ✅ Аудио работает
   - ✅ Соединение стабильное (не обрывается)
   - ✅ При открытии настроек панель с эмодзи не появляется

---

## 🔄 Если что-то пошло не так - откат изменений

```bash
# Восстанавливаем бэкапы
cp meet.super2026.online-config.js.backup meet.super2026.online-config.js
cp meet.super2026.online-interface_config.js.backup meet.super2026.online-interface_config.js.backup

# Перезапускаем сервисы
systemctl restart jitsi-videobridge2
systemctl restart prosody
systemctl restart jicofo
systemctl reload nginx
```

---

## 📝 Примечания

- **Не удаляйте существующие настройки** - только добавляйте новые
- **Сохраняйте отступы** - используйте пробелы или табы как в оригинале
- **Проверяйте запятые** - после последнего элемента в объекте запятой быть не должно
- **Если не уверены** - лучше спросить перед сохранением

---

**Готовы начать? Выполните Шаг 1 и сообщите результат!**

