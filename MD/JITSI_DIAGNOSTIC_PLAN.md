# 🔍 План диагностики и восстановления Jitsi

## 📋 Шаг 1: Диагностика текущего состояния

### A. Проверка на сервере (выполнить на сервере)

```bash
# 1. Проверить установленные пакеты Jitsi
dpkg -l | grep jitsi

# 2. Проверить статус сервисов Jitsi
systemctl status prosody
systemctl status jitsi-videobridge2
systemctl status jicofo
systemctl status nginx

# 3. Проверить конфигурацию nginx
ls -la /etc/nginx/sites-enabled/ | grep meet
cat /etc/nginx/sites-enabled/meet.super2026.online.conf 2>/dev/null || echo "Файл не найден"

# 4. Проверить конфигурационные файлы Jitsi
ls -la /etc/jitsi/meet/ 2>/dev/null || echo "Директория не найдена"
ls -la /var/lib/prosody/*meet* 2>/dev/null || echo "Сертификаты не найдены"

# 5. Проверить переменные окружения в приложении
cd /root/cosmos-of-hopes
cat .env.local | grep JITSI || echo "Переменная не найдена"

# 6. Проверить доступность Jitsi сервера
curl -I https://meet.super2026.online 2>&1 | head -10
```

### B. Проверка в коде (локально)

Проблема в `VideoRoom.tsx`:
- ❌ Используется `setIsLoading` но не объявлен `isLoading` в useState
- ✅ Код в `app/room/page.tsx` использует VideoRoom правильно

---

## 🎯 Варианты восстановления

### Вариант 1: Быстрое исправление (если Jitsi установлен, но не работает)

**Плюсы:** Быстро, не нужно переустанавливать
**Минусы:** Могут остаться проблемы из предыдущей установки

**План:**
1. Исправить ошибку в коде `VideoRoom.tsx` (добавить `isLoading` в useState)
2. Проверить и исправить конфигурацию nginx для Jitsi
3. Проверить и пересоздать конфигурационный файл Jitsi
4. Проверить работу сервисов
5. Обновить приложение на сервере

### Вариант 2: Полная переустановка (чистый старт)

**Плюсы:** Чистая установка, все проблемы будут решены
**Минусы:** Займет больше времени (~30-40 минут)

**План:**
1. Удалить все пакеты Jitsi
2. Удалить все конфигурации Jitsi
3. Очистить переменные debconf
4. Установить Jitsi заново с правильными настройками
5. Исправить код `VideoRoom.tsx`
6. Настроить SSL сертификат
7. Обновить приложение

### Вариант 3: Временное отключение Jitsi (вернуться к публичному серверу)

**Плюсы:** Быстро восстановит работу приложения
**Минусы:** Нет собственного сервера

**План:**
1. Отключить конфигурацию nginx для Jitsi
2. Удалить переменную `NEXT_PUBLIC_JITSI_SERVER_URL` из `.env.local`
3. Исправить ошибку в `VideoRoom.tsx`
4. Приложение будет использовать `meet.jit.si` (публичный сервер)

---

## 🔧 Что обязательно нужно исправить в коде

**Файл:** `components/rooms/VideoRoom.tsx`

**Проблема:** В строке 34 используется `setIsLoading(true)`, но `isLoading` не объявлен в useState.

**Исправление:**
```typescript
// ДО (строка 17-19):
const [isLoading, setIsLoading] = useState(true);  // ✅ Есть
const [error, setError] = useState<string | null>(null);

// НО в строке 61 есть еще один useState для isFullscreen который объявлен ПОСЛЕ использования setIsLoading
// Это неправильный порядок!
```

Нужно переместить все useState в начало компонента.

---

## 📊 Рекомендация

**Сначала:** Выполнить диагностику (Шаг 1), чтобы понять что именно сломалось.

**Затем выбрать вариант:**
- Если Jitsi установлен и просто не работает → Вариант 1
- Если установка полностью сломана → Вариант 2  
- Если нужно быстро восстановить работу → Вариант 3

---

**Дата:** 2025-01-28

