# 🔧 Исправление приоритета конфигураций nginx

## Проблема
Конфигурация правильная, но запросы не проксируются правильно.

## Решение

### Шаг 1: Переименовать конфигурацию cosmos-of-hopes для приоритета

Nginx загружает конфигурации в алфавитном порядке. Нужно, чтобы cosmos-of-hopes загружалась ПЕРВОЙ:

```bash
# Удалить текущий симлинк
rm /etc/nginx/sites-enabled/cosmos-of-hopes

# Создать новый симлинк с префиксом для приоритета
ln -sf /etc/nginx/sites-available/cosmos-of-hopes /etc/nginx/sites-enabled/00-cosmos-of-hopes

# Переименовать конфигурацию Jitsi, чтобы загружалась последней
mv /etc/nginx/sites-enabled/meet.super2026.online.conf /etc/nginx/sites-enabled/zzz-meet.super2026.online.conf

# Проверить порядок
ls -la /etc/nginx/sites-enabled/
```

### Шаг 2: Проверить конфигурацию

```bash
nginx -t
```

Должно быть: `nginx: configuration file /etc/nginx/nginx.conf test is successful`

### Шаг 3: Перезагрузить nginx

```bash
systemctl reload nginx
```

### Шаг 4: Проверить работу

```bash
# Проверить локально
curl -v -H "Host: super2026.online" http://localhost 2>&1 | head -40

# Должен вернуть HTML вашего приложения, а не редирект
```

### Шаг 5: Если проблема осталась - проверить полную конфигурацию

```bash
# Посмотреть, какой server блок обрабатывает запрос
nginx -T 2>/dev/null | grep -A 5 "listen 80" | head -50
```

### Шаг 6: Альтернативное решение - добавить приоритет через default_server

Если переименование не помогло, можно явно указать default_server:

```bash
# Отредактировать конфигурацию
nano /etc/nginx/sites-available/cosmos-of-hopes
```

Изменить строку:
```nginx
listen 80;
```

На:
```nginx
listen 80 default_server;
```

Сохранить (Ctrl+O, Enter, Ctrl+X) и:

```bash
# Проверить
nginx -t

# Перезагрузить
systemctl reload nginx

# Проверить работу
curl -v -H "Host: super2026.online" http://localhost 2>&1 | head -40
```

## Финальная проверка

После исправления проверьте:

```bash
# 1. Порядок конфигураций
ls -la /etc/nginx/sites-enabled/

# 2. Тест локально
curl -I -H "Host: super2026.online" http://localhost

# 3. Тест с реального домена (если есть доступ)
curl -I http://super2026.online

# 4. Проверить логи
tail -10 /var/log/nginx/access.log
tail -10 /var/log/nginx/error.log
```

