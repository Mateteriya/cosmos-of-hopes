# 📱 НАСТРОЙКА СЕРВЕРА НА ANDROID-ПЛАНШЕТЕ

## 📊 ОЦЕНКА ВОЗМОЖНОСТЕЙ ПЛАНШЕТА

### ✅ Технические характеристики Huawei MediaPad M5 Lite 10

**Плюсы:**
- ✅ 3 ГБ RAM - достаточно для легкого Next.js сервера
- ✅ 8-ядерный процессор Kirin 659 (2.36 ГГц) - справится с нагрузкой
- ✅ 32 ГБ памяти + слот microSD до 256 ГБ - места хватит
- ✅ LTE поддержка - стабильный интернет
- ✅ 7500 мАч батарея - при постоянной зарядке проработает долго
- ✅ Android 8.0 - можно установить Termux

**Минусы и риски:**
- ⚠️ Android может убивать фоновые процессы
- ⚠️ Планшет может перегреваться при постоянной работе
- ⚠️ Батарея деградирует при постоянной зарядке
- ⚠️ Нет гарантии работы 24/7 без сбоев
- ⚠️ Ограниченная производительность по сравнению с VPS

---

## 🎯 РЕКОМЕНДУЕМАЯ АРХИТЕКТУРА

### Вариант 1: Гибридный подход (РЕКОМЕНДУЕТСЯ)

**Планшет как резервный/дополнительный сервер:**
- Основной сервер остается на VPS (стабильность)
- Планшет работает параллельно для сбора данных
- Автоматическое переключение при сбое основного

**Преимущества:**
- ✅ Надежность (если планшет упадет, основной сервер работает)
- ✅ Можно тестировать обновления на планшете
- ✅ Дополнительная точка сбора данных

### Вариант 2: Только планшет (РИСКОВАННО)

**Планшет как единственный сервер:**
- Все работает только на планшете
- Требует максимальной настройки защиты

**Риски:**
- ❌ Если планшет упадет - приложение недоступно
- ❌ Нет резерва
- ❌ Сложнее обеспечить стабильность

---

## 🛠️ ПОШАГОВАЯ НАСТРОЙКА ПЛАНШЕТА

### Шаг 1: Установка Termux

1. **Скачать Termux:**
   - Из F-Droid (рекомендуется) или Google Play
   - Версия должна поддерживать Android 8.0

2. **Настройка Termux:**
   ```bash
   # Обновление пакетов
   pkg update && pkg upgrade
   
   # Установка необходимых инструментов
   pkg install nodejs npm git curl wget
   ```

### Шаг 2: Установка Node.js

```bash
# Проверка версии Node.js
node --version

# Если версия старая, установить через nvm
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash
source ~/.bashrc
nvm install 18
nvm use 18
```

### Шаг 3: Клонирование проекта

```bash
# Переход в домашнюю директорию
cd ~

# Клонирование проекта (или копирование через USB)
git clone <ваш-репозиторий> cosmos-of-hopes
# ИЛИ скопировать файлы через USB OTG

cd cosmos-of-hopes

# Установка зависимостей
npm install
```

### Шаг 4: Настройка переменных окружения

```bash
# Создать .env.local
nano .env.local
```

**Содержимое .env.local:**
```env
NEXT_PUBLIC_SUPABASE_URL=ваш_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=ваш_anon_key
NEXT_PUBLIC_APP_URL=http://ваш-ip-планшета:3000
NEXT_PUBLIC_JITSI_SERVER_URL=https://meet.jit.si
PORT=3000
```

### Шаг 5: Сборка приложения

```bash
# Сборка production версии
npm run build

# Проверка, что сборка прошла успешно
ls -la .next
```

---

## 🔋 ОБЕСПЕЧЕНИЕ БЕСПЕРЕБОЙНОЙ РАБОТЫ

### 1. Защита от засыпания Android

**Настройки планшета:**
1. **Настройки → Экран → Спящий режим** → "Никогда"
2. **Настройки → Батарея → Оптимизация батареи** → Отключить для Termux
3. **Настройки → Приложения → Termux → Батарея** → "Не оптимизировать"
4. **Настройки → Приложения → Termux → Автозапуск** → Включить

**Приложения для защиты:**
- **Wakelock** - предотвращает засыпание
- **Tasker** - автоматизация (платное, но мощное)
- **MacroDroid** - бесплатная альтернатива Tasker

### 2. Постоянная зарядка

**ВАЖНО:**
- ⚠️ Постоянная зарядка может повредить батарею
- ✅ Использовать **умную зарядку** (останавливается на 80-90%)
- ✅ Или использовать **USB OTG + внешний аккумулятор**
- ✅ Или подключить к зарядному устройству с контролем заряда

**Рекомендация:**
- Использовать зарядное устройство с функцией "защита батареи"
- Или настроить Tasker для остановки зарядки на 85%

### 3. Автозапуск сервера

**Создать скрипт автозапуска:**

```bash
# Создать файл автозапуска
nano ~/start-server.sh
```

**Содержимое start-server.sh:**
```bash
#!/bin/bash

# Переход в директорию проекта
cd ~/cosmos-of-hopes

# Проверка, запущен ли уже сервер
if pgrep -f "next start" > /dev/null; then
    echo "Сервер уже запущен"
    exit 0
fi

# Запуск сервера
npm start > ~/server.log 2>&1 &

# Сохранение PID
echo $! > ~/server.pid
echo "Сервер запущен, PID: $!"
```

**Сделать исполняемым:**
```bash
chmod +x ~/start-server.sh
```

**Добавить в автозагрузку Termux:**
```bash
# Создать файл .bashrc если его нет
nano ~/.bashrc
```

**Добавить в конец .bashrc:**
```bash
# Автозапуск сервера
if [ ! -f ~/server.pid ] || ! pgrep -F ~/server.pid > /dev/null; then
    ~/start-server.sh
fi
```

### 4. Мониторинг и автоперезапуск

**Создать скрипт мониторинга:**

```bash
nano ~/monitor-server.sh
```

**Содержимое monitor-server.sh:**
```bash
#!/bin/bash

cd ~/cosmos-of-hopes

# Проверка доступности сервера
if ! curl -f http://localhost:3000 > /dev/null 2>&1; then
    echo "$(date): Сервер не отвечает, перезапускаю..." >> ~/server-monitor.log
    
    # Убить старый процесс если есть
    if [ -f ~/server.pid ]; then
        kill $(cat ~/server.pid) 2>/dev/null
        rm ~/server.pid
    fi
    
    # Запустить заново
    ~/start-server.sh
fi
```

**Сделать исполняемым:**
```bash
chmod +x ~/monitor-server.sh
```

**Настроить cron для проверки каждые 5 минут:**
```bash
# Установить cron
pkg install cronie

# Открыть crontab
crontab -e
```

**Добавить строку:**
```
*/5 * * * * ~/monitor-server.sh
```

---

## 🔒 ЗАЩИТА И БЕЗОПАСНОСТЬ

### 1. Firewall (Брандмауэр)

**Настройка через Termux:**
```bash
# Установка iptables (если доступен root)
# Или использовать встроенный Android Firewall

# Ограничить доступ только к нужным портам
# (требует root доступ)
```

**Без root:**
- Использовать VPN для защиты
- Настроить Supabase RLS (Row Level Security)
- Использовать только HTTPS (через reverse proxy)

### 2. Резервное копирование

**Автоматическое резервное копирование:**

```bash
nano ~/backup-server.sh
```

**Содержимое backup-server.sh:**
```bash
#!/bin/bash

BACKUP_DIR=~/backups
DATE=$(date +%Y%m%d_%H%M%S)

# Создать директорию для бэкапов
mkdir -p $BACKUP_DIR

# Бэкап проекта
tar -czf $BACKUP_DIR/cosmos-backup-$DATE.tar.gz ~/cosmos-of-hopes

# Бэкап конфигурации
cp ~/cosmos-of-hopes/.env.local $BACKUP_DIR/env-$DATE.txt

# Удалить старые бэкапы (оставить последние 7)
find $BACKUP_DIR -name "*.tar.gz" -mtime +7 -delete

echo "Бэкап создан: $BACKUP_DIR/cosmos-backup-$DATE.tar.gz"
```

**Добавить в cron (ежедневно в 3:00):**
```bash
crontab -e
# Добавить:
0 3 * * * ~/backup-server.sh
```

### 3. Синхронизация с облаком

**Варианты:**
- **Yandex.Disk** (у вас уже есть папка "ПроТекст")
- **Google Drive** (через rclone)
- **Dropbox** (через API)

**Настройка синхронизации Yandex.Disk:**

```bash
# Установка yandex-disk (если доступен)
# Или использовать скрипт загрузки через API

# Простой скрипт загрузки бэкапа
nano ~/upload-backup.sh
```

---

## 🌐 ДОСТУП ИЗВНЕ

### Вариант 1: Прямой доступ по IP (простой, но небезопасный)

**Настройка:**
1. Узнать IP планшета в локальной сети
2. Настроить проброс портов на роутере
3. Использовать динамический DNS (если IP меняется)

**Риски:**
- ⚠️ Небезопасно без HTTPS
- ⚠️ Нужен статический IP или DDNS

### Вариант 2: Туннелирование (рекомендуется)

**Использовать ngrok или Cloudflare Tunnel:**

```bash
# Установка ngrok
pkg install ngrok

# Запуск туннеля
ngrok http 3000
```

**Или Cloudflare Tunnel (бесплатно, стабильнее):**
```bash
# Установка cloudflared
pkg install cloudflared

# Запуск туннеля
cloudflared tunnel --url http://localhost:3000
```

### Вариант 3: Reverse Proxy через VPS

**Схема:**
```
Пользователи → VPS (nginx) → Планшет (через туннель)
```

**Преимущества:**
- ✅ HTTPS через VPS
- ✅ Стабильный домен
- ✅ Планшет скрыт от прямого доступа

---

## 📊 МОНИТОРИНГ И ЛОГИ

### Просмотр логов

```bash
# Логи сервера
tail -f ~/server.log

# Логи мониторинга
tail -f ~/server-monitor.log

# Системные логи
logcat | grep -i termux
```

### Проверка состояния

```bash
# Проверка процессов
ps aux | grep node

# Проверка портов
netstat -tuln | grep 3000

# Использование памяти
free -h

# Использование диска
df -h
```

---

## ⚠️ КРИТИЧЕСКИЕ РИСКИ И РЕШЕНИЯ

### Риск 1: Планшет перезагрузится

**Решение:**
- ✅ Автозапуск через .bashrc
- ✅ Мониторинг и автоперезапуск
- ✅ Резервный сервер на VPS

### Риск 2: Батарея разрядится

**Решение:**
- ✅ Постоянная зарядка
- ✅ Умное зарядное устройство
- ✅ Мониторинг уровня батареи

### Риск 3: Android убьет процесс

**Решение:**
- ✅ Отключить оптимизацию батареи
- ✅ Использовать Wakelock
- ✅ Регулярный мониторинг

### Риск 4: Интернет пропадет

**Решение:**
- ✅ Использовать LTE как основной
- ✅ Wi-Fi как резервный
- ✅ Автоматическое переключение

### Риск 5: Перегрев

**Решение:**
- ✅ Охлаждающая подставка
- ✅ Хорошая вентиляция
- ✅ Мониторинг температуры

---

## 🎯 РЕКОМЕНДАЦИИ

### Для вашего случая (сбор данных до 1 января 2027):

1. **Использовать гибридный подход:**
   - Основной сервер на VPS (стабильность)
   - Планшет как дополнительный сборщик данных

2. **Настроить автоматическую синхронизацию:**
   - Данные с планшета → Supabase
   - Резервное копирование в Yandex.Disk

3. **Мониторинг:**
   - Проверять состояние раз в неделю
   - Настроить уведомления о сбоях

4. **Защита:**
   - Использовать HTTPS через reverse proxy
   - Регулярные бэкапы
   - Мониторинг логов

---

## 📝 ЧЕКЛИСТ НАСТРОЙКИ

- [ ] Установлен Termux
- [ ] Установлен Node.js 18+
- [ ] Скопирован проект на планшет
- [ ] Установлены зависимости (npm install)
- [ ] Настроен .env.local
- [ ] Собрано приложение (npm run build)
- [ ] Настроен автозапуск
- [ ] Настроен мониторинг
- [ ] Настроено резервное копирование
- [ ] Отключена оптимизация батареи
- [ ] Настроена постоянная зарядка
- [ ] Настроен доступ извне (туннель/VPS)
- [ ] Протестирована работа сервера
- [ ] Настроены уведомления о сбоях

---

## 🚀 БЫСТРЫЙ СТАРТ

После настройки Termux:

```bash
# 1. Установка зависимостей
pkg update && pkg upgrade
pkg install nodejs npm git curl wget cronie

# 2. Клонирование/копирование проекта
cd ~
# (скопировать проект)

# 3. Установка зависимостей проекта
cd cosmos-of-hopes
npm install

# 4. Настройка .env.local
nano .env.local

# 5. Сборка
npm run build

# 6. Настройка автозапуска
# (следовать инструкциям выше)

# 7. Запуск
npm start
```

---

## 📞 ПОДДЕРЖКА

При возникновении проблем:
1. Проверить логи: `tail -f ~/server.log`
2. Проверить процессы: `ps aux | grep node`
3. Проверить порт: `netstat -tuln | grep 3000`
4. Перезапустить: `~/start-server.sh`

