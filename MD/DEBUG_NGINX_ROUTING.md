# 🔍 Диагностика маршрутизации nginx

## Приложение запущено на порту 3000 ✅

Теперь нужно проверить, почему nginx не проксирует запросы правильно.

## Шаг 1: Проверить, что приложение отвечает локально

```bash
# Проверить, что приложение отвечает на localhost:3000
curl -I http://localhost:3000

# Должен вернуть HTTP 200 и заголовки
```

## Шаг 2: Проверить порядок загрузки конфигураций

```bash
# Посмотреть порядок файлов в sites-enabled
ls -la /etc/nginx/sites-enabled/

# Nginx загружает конфигурации в алфавитном порядке!
# Если meet.super2026.online.conf загружается раньше, может быть конфликт
```

## Шаг 3: Проверить, нет ли конфликта server_name

```bash
# Проверить все server_name в активных конфигурациях
grep -r "server_name" /etc/nginx/sites-enabled/ | grep -v "#"

# Убедиться, что:
# - cosmos-of-hopes имеет server_name super2026.online
# - meet.super2026.online.conf имеет ТОЛЬКО server_name meet.super2026.online
```

## Шаг 4: Проверить логи nginx

```bash
# Посмотреть последние ошибки nginx
tail -50 /var/log/nginx/error.log

# Посмотреть последние запросы
tail -50 /var/log/nginx/access.log
```

## Шаг 5: Проверить, какой server блок обрабатывает запрос

```bash
# Тест конфигурации с подробным выводом
nginx -T 2>/dev/null | grep -A 20 "server_name.*super2026.online" | head -100
```

## Шаг 6: Временный тест - добавить приоритет

Проблема может быть в том, что конфигурация Jitsi имеет более высокий приоритет.

Попробуйте переименовать файлы, чтобы cosmos-of-hopes загружался первым:

```bash
# Создать резервную копию
cp /etc/nginx/sites-enabled/meet.super2026.online.conf /etc/nginx/sites-enabled/zzz-meet.super2026.online.conf

# Удалить оригинальный файл из sites-enabled
rm /etc/nginx/sites-enabled/meet.super2026.online.conf

# Перезагрузить nginx
nginx -t
systemctl reload nginx

# Проверить работу
curl -I http://super2026.online
```

Если это помогло - значит проблема в порядке загрузки. Тогда нужно будет правильно настроить приоритеты.

## Шаг 7: Проверить, что конфигурация cosmos-of-hopes действительно активна

```bash
# Проверить симлинк
ls -la /etc/nginx/sites-enabled/cosmos-of-hopes

# Должен быть: cosmos-of-hopes -> /etc/nginx/sites-available/cosmos-of-hopes

# Проверить содержимое активной конфигурации
cat /etc/nginx/sites-enabled/cosmos-of-hopes
```

## Шаг 8: Тест с curl напрямую

```bash
# Проверить, что nginx проксирует запросы
curl -H "Host: super2026.online" http://localhost

# Должен вернуть HTML вашего приложения, а не Jitsi
```

## Шаг 9: Проверить default конфигурацию

```bash
# Проверить, не перехватывает ли default конфигурация
cat /etc/nginx/sites-enabled/default 2>/dev/null | grep -A 10 "server_name"

# Если default имеет server_name _ или *, он может перехватывать все запросы
```

## Если ничего не помогает

Выполните и пришлите полный вывод:

```bash
echo "=== Все активные конфигурации ==="
ls -la /etc/nginx/sites-enabled/

echo "=== Содержимое cosmos-of-hopes ==="
cat /etc/nginx/sites-enabled/cosmos-of-hopes

echo "=== Все server_name ==="
grep -r "server_name" /etc/nginx/sites-enabled/ | grep -v "#"

echo "=== Тест приложения локально ==="
curl -I http://localhost:3000

echo "=== Тест через nginx ==="
curl -I -H "Host: super2026.online" http://localhost

echo "=== Логи nginx (последние 20 строк) ==="
tail -20 /var/log/nginx/error.log
tail -20 /var/log/nginx/access.log
```

