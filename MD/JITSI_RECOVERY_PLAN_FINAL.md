# 🔍 ИТОГОВЫЙ ПЛАН: Диагностика и восстановление Jitsi

## ❗ КРИТИЧЕСКАЯ ОШИБКА В КОДЕ

**Файл:** `components/rooms/VideoRoom.tsx`

**Проблема:** Строка 61 - `useState` для `isFullscreen` объявлен **ПОСЛЕ** условного возврата `if (error)` (строка 46).

**Это нарушает правила React Hooks!** Все хуки должны быть в начале компонента, до любых условных возвратов.

**Ошибка будет:** "React Hook "useState" is called conditionally" или компонент может не работать правильно.

---

## 📋 ШАГ 1: ДИАГНОСТИКА (выполнить на сервере)

**Выполните эти команды по порядку и пришлите результаты:**

```bash
# 1. Проверить установленные пакеты Jitsi
dpkg -l | grep jitsi

# 2. Проверить статус сервисов
systemctl status prosody --no-pager | head -15
systemctl status jitsi-videobridge2 --no-pager | head -15
systemctl status jicofo --no-pager | head -15
systemctl status nginx --no-pager | head -15

# 3. Проверить конфигурацию nginx
ls -la /etc/nginx/sites-enabled/

# 4. Проверить есть ли конфигурация Jitsi
cat /etc/nginx/sites-enabled/meet.super2026.online.conf 2>/dev/null | head -50 || echo "Файл не найден"

# 5. Проверить конфигурационные файлы Jitsi
ls -la /etc/jitsi/meet/ 2>/dev/null || echo "Директория не найдена"

# 6. Проверить переменную окружения
cd /root/cosmos-of-hopes
grep JITSI .env.local 2>/dev/null || echo "Переменная не найдена"

# 7. Проверить доступность Jitsi
curl -I https://meet.super2026.online 2>&1 | head -10

# 8. Проверить логи nginx на ошибки
tail -30 /var/log/nginx/error.log 2>/dev/null | tail -20
```

---

## 🎯 ПЛАН ДЕЙСТВИЙ (после диагностики)

### ⚡ ВАРИАНТ A: Быстрое исправление (если Jitsi установлен)

**Когда:** Сервисы работают, но есть конфликты или ошибки конфигурации

**План:**

1. ✅ **Исправить код** (обязательно в любом случае!)
   - Переместить `useState` для `isFullscreen` в начало компонента

2. 🔧 **Исправить на сервере:**
   - Проверить и исправить конфигурацию nginx
   - Проверить конфигурационный файл Jitsi
   - Перезапустить сервисы если нужно

3. 🔄 **Обновить приложение:**
   - Закоммитить исправления
   - На сервере: `git pull`, `npm run build`, `pm2 restart`

**Время:** ~15-20 минут

---

### 🔄 ВАРИАНТ B: Полная переустановка

**Когда:** Установка полностью сломана, пакеты в неконсистентном состоянии

**План:**

1. 🗑️ **Полностью удалить Jitsi:**
   ```bash
   # Удалить пакеты
   apt remove --purge jitsi-meet jitsi-meet-prosody jitsi-meet-web jitsi-meet-web-config jitsi-meet-turnserver jicofo jitsi-videobridge2 prosody coturn
   
   # Удалить все конфигурации
   rm -rf /etc/jitsi
   rm -rf /etc/prosody/conf.avail/*meet*
   rm -rf /var/lib/prosody/*meet*
   rm -f /etc/nginx/sites-enabled/*meet*
   rm -f /etc/nginx/sites-available/*meet*
   
   # Очистить debconf
   echo "jitsi-videobridge jitsi-videobridge/jvb-hostname string meet.super2026.online" | debconf-set-selections
   ```

2. 📦 **Установить заново:**
   - Следовать инструкции `JITSI_QUICK_START.md`
   - Использовать домен БЕЗ кавычек: `meet.super2026.online`

3. ✅ **Исправить код** (обязательно!)

4. 🔄 **Обновить приложение**

**Время:** ~30-40 минут

---

### 🔙 ВАРИАНТ C: Вернуться к публичному серверу (быстро)

**Когда:** Нужно быстро восстановить работу, свой сервер не критичен

**План:**

1. 🔕 **Отключить Jitsi на сервере:**
   ```bash
   # Отключить конфигурацию nginx
   mv /etc/nginx/sites-enabled/meet.super2026.online.conf /etc/nginx/sites-enabled/meet.super2026.online.conf.disabled 2>/dev/null
   systemctl reload nginx
   ```

2. 🔧 **Удалить переменную окружения:**
   ```bash
   cd /root/cosmos-of-hopes
   # Отредактировать .env.local - удалить или закомментировать строку:
   # NEXT_PUBLIC_JITSI_SERVER_URL=https://meet.super2026.online
   nano .env.local
   ```

3. ✅ **Исправить код** (обязательно!)

4. 🔄 **Обновить приложение:**
   ```bash
   git pull origin main
   npm run build
   pm2 restart cosmos-of-hopes --update-env
   ```

**Время:** ~10 минут  
**Результат:** Используется `meet.jit.si` (публичный сервер)

---

## ✅ ЧТО ОБЯЗАТЕЛЬНО НУЖНО ИСПРАВИТЬ В КОДЕ

**Файл:** `components/rooms/VideoRoom.tsx`

**Изменение:** Переместить строку 61 `const [isFullscreen, setIsFullscreen] = useState(false);` в начало компонента (после строки 19, вместе с другими useState).

**Правильный порядок:**
```typescript
export default function VideoRoom({ roomId, currentUserId, displayName }: VideoRoomProps) {
  const [isLoading, setIsLoading] = useState(true);
  const [error, setError] = useState<string | null>(null);
  const [isFullscreen, setIsFullscreen] = useState(false);  // ← ПЕРЕМЕСТИТЬ СЮДА
  const iframeRef = useRef<HTMLIFrameElement>(null);
  
  // ... остальной код ...
  
  // if (error) { return ... } ← условный возврат должен быть ПОСЛЕ всех хуков
}
```

---

## 🎯 РЕКОМЕНДАЦИЯ

1. **Сначала:** Выполнить диагностику (команды выше) - это покажет точную проблему

2. **Параллельно:** Исправить ошибку в коде (это нужно в любом случае)

3. **После диагностики выбрать вариант:**
   - Если сервисы работают → Вариант A
   - Если всё сломано → Вариант B
   - Если нужно быстро → Вариант C

---

## 📝 Следующие шаги

1. Выполните команды диагностики на сервере
2. Пришлите результаты
3. Я определю точный план восстановления
4. Исправим код (обязательно!)
5. Восстановим или переустановим Jitsi

---

**Дата:** 2025-01-28

