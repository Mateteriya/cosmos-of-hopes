# 🔔 Исправление уведомлений в продакшн

## Проблема

Уведомления работают только в локальной версии, но не работают в продакшн.

## Решение

### 1. Проверьте переменные окружения на сервере

Убедитесь, что на продакшн сервере настроены переменные окружения:

#### На сервере выполните:

```bash
# Перейдите в папку проекта
cd /path/to/cosmos-of-hopes

# Проверьте, есть ли файл .env.local
ls -la .env.local

# Если файла нет или нужно обновить - откройте его
nano .env.local
```

#### Добавьте в `.env.local` на сервере:

```env
NEXT_PUBLIC_VAPID_PUBLIC_KEY=ваш_public_ключ_здесь
VAPID_PRIVATE_KEY=ваш_private_ключ_здесь
NEXT_PUBLIC_SUPABASE_URL=ваш_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=ваш_anon_key
SUPABASE_SERVICE_ROLE_KEY=ваш_service_role_key
NEXT_PUBLIC_APP_URL=https://ваш-домен.com
```

⚠️ **ВАЖНО**: Если у вас нет VAPID ключей, сгенерируйте их:

```bash
# На локальной машине
cd cosmos-of-hopes
npm install web-push
npm run generate-vapid-keys
```

Скопируйте ключи и добавьте их в `.env.local` на сервере.

### 2. Пересоберите приложение на сервере

После добавления переменных окружения:

```bash
# На сервере
cd /path/to/cosmos-of-hopes

# Установите зависимости (если нужно)
npm install

# Соберите приложение заново
npm run build

# Перезапустите приложение
# Если используете PM2:
pm2 restart cosmos-of-hopes

# Или если используете systemd:
sudo systemctl restart cosmos-of-hopes

# Или если запускаете напрямую:
npm run start
```

### 3. Проверьте, что Service Worker доступен

Откройте в браузере:
```
https://ваш-домен.com/sw.js
```

Должен открыться JavaScript файл (Service Worker). Если открывается HTML страница или ошибка 404 - значит файл не доступен.

### 4. Проверьте Nginx конфигурацию (если используется)

Убедитесь, что Nginx правильно обслуживает статические файлы из папки `.next`:

```nginx
server {
    listen 80;
    server_name ваш-домен.com;

    # Статические файлы (включая Service Worker)
    location /sw.js {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        add_header Cache-Control "public, max-age=0, must-revalidate";
    }

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}
```

После изменения конфигурации:

```bash
# Проверьте конфигурацию
sudo nginx -t

# Перезагрузите Nginx
sudo systemctl reload nginx
```

### 5. Проверьте HTTPS

⚠️ **КРИТИЧЕСКИЙ МОМЕНТ**: Service Worker и Push Notifications **ТРЕБУЮТ HTTPS**!

Убедитесь, что:
- Ваш сайт доступен по HTTPS: `https://ваш-домен.com`
- У вас настроен SSL сертификат (например, через Let's Encrypt)

Для локального тестирования можно использовать:
- `localhost` (работает без HTTPS)
- `127.0.0.1` (работает без HTTPS)

Но для реальных пользователей **ОБЯЗАТЕЛЕН HTTPS**!

### 6. Проверка в браузере

1. Откройте сайт в браузере
2. Откройте DevTools (F12)
3. Перейдите на вкладку **Application** (или **Приложение**)
4. В левом меню выберите **Service Workers**
5. Должен быть виден зарегистрированный Service Worker для вашего домена
6. Проверьте вкладку **Console** на наличие ошибок

### 7. Проверка переменных окружения в продакшн

Чтобы убедиться, что переменные окружения правильно загружаются, можно временно добавить в код проверку:

```typescript
// В lib/pushNotifications.ts уже есть проверка
// Если VAPID_PUBLIC_KEY пустой - в консоли браузера будет предупреждение
```

Проверьте консоль браузера (F12 → Console) на наличие сообщений:
- `[Push Notifications] VAPID public key not configured` - значит переменная окружения не загружена
- `[Push Notifications] Service Worker registered` - значит всё работает

## Быстрая проверка

Выполните на сервере эти команды для быстрой диагностики:

```bash
# 1. Проверьте, что файл .env.local существует
ls -la .env.local

# 2. Проверьте, что переменные окружения содержат значения (не пустые)
grep "NEXT_PUBLIC_VAPID_PUBLIC_KEY" .env.local
grep "VAPID_PRIVATE_KEY" .env.local

# 3. Проверьте, что Service Worker файл существует
ls -la public/sw.js

# 4. Проверьте, что приложение собрано
ls -la .next/

# 5. Проверьте статус приложения
pm2 status
# или
sudo systemctl status cosmos-of-hopes
```

## Типичные проблемы

### Проблема: Service Worker не регистрируется

**Решение:**
1. Убедитесь, что сайт доступен по HTTPS (не HTTP)
2. Проверьте, что файл `/sw.js` доступен по URL
3. Проверьте консоль браузера на ошибки

### Проблема: "VAPID public key not configured"

**Решение:**
1. Убедитесь, что переменная `NEXT_PUBLIC_VAPID_PUBLIC_KEY` добавлена в `.env.local` на сервере
2. Пересоберите приложение (`npm run build`)
3. Перезапустите приложение

### Проблема: Уведомления не появляются

**Решение:**
1. Проверьте, что пользователь разрешил уведомления в браузере
2. Проверьте настройки браузера: сайт должен иметь разрешение на уведомления
3. Проверьте, что Service Worker зарегистрирован (см. пункт 6 выше)

## После исправления

После выполнения всех шагов:
1. Очистите кеш браузера (Ctrl+Shift+Delete)
2. Перезагрузите страницу (Ctrl+F5)
3. Попробуйте включить уведомления снова
4. Проверьте консоль браузера на ошибки

