# 🔧 Исправление ошибок установки Jitsi

## Проблема
Домен указан с обратными кавычками (`meet.super2026.online`) вместо обычного текста, что вызывает ошибки.

---

## 🔍 Шаг 1: Проверить и исправить конфигурацию Prosody

Подключитесь к серверу и выполните:

```bash
ssh root@5.129.223.23

# Проверить файлы конфигурации Prosody
ls -la /etc/prosody/conf.d/
ls -la /etc/prosody/conf.avail/
```

Найдите файл с доменом (скорее всего `meet.super2026.online.cfg.lua` или подобный) и проверьте, нет ли там обратных кавычек.

---

## 🔧 Шаг 2: Исправить конфигурацию Jitsi Prosody

```bash
# Проверить конфигурацию
cat /etc/prosody/conf.avail/meet.super2026.online.cfg.lua

# Если видите обратные кавычки, удалите файл и пересоздайте
# Сначала проверим, что там
grep -r "meet.super2026.online" /etc/prosody/
```

Если найдете файлы с неправильными кавычками, нужно их исправить.

---

## 🛠️ Шаг 3: Исправить проблему с пакетами

### Вариант A: Переконфигурировать пакеты

```bash
# Попытаться исправить конфигурацию пакетов
dpkg --configure -a

# Если не поможет, удалить проблемные конфигурации
rm -rf /etc/prosody/conf.avail/*meet*
rm -rf /var/lib/prosody/*meet*
```

### Вариант B: Полная переустановка (если вариант A не поможет)

```bash
# Удалить пакеты Jitsi
apt remove --purge jitsi-meet jitsi-meet-prosody jitsi-meet-web jitsi-meet-web-config jitsi-meet-turnserver jicofo jitsi-videobridge2

# Очистить конфигурации
rm -rf /etc/jitsi
rm -rf /etc/prosody/conf.avail/*meet*
rm -rf /var/lib/prosody/*meet*

# Переустановить
apt update
apt install -y jitsi-meet
```

**ВНИМАНИЕ:** При установке введите домен БЕЗ КАВЫЧЕК: `meet.super2026.online`

---

## 🔍 Шаг 4: Проверить статус Nginx

```bash
# Проверить статус
systemctl status nginx

# Посмотреть логи
journalctl -xeu nginx.service

# Проверить конфигурацию
nginx -t
```

Если есть ошибки в конфигурации nginx, исправьте их.

---

## 🔒 Шаг 5: Установить SSL сертификат (после исправления)

После того как пакеты установлены правильно:

```bash
# Установить SSL сертификат (замените email на ваш)
/usr/share/jitsi-meet/scripts/install-letsencrypt-cert.sh

# При запросе введите домен БЕЗ КАВЫЧЕК: meet.super2026.online
```

---

## ⚠️ ВАЖНО: Как правильно вводить домен

**НЕПРАВИЛЬНО:**
- `meet.super2026.online` (с обратными кавычками)
- 'meet.super2026.online' (с одинарными кавычками)
- "meet.super2026.online" (с двойными кавычками)

**ПРАВИЛЬНО:**
- meet.super2026.online (БЕЗ КАВЫЧЕК!)

---

## 🚀 Быстрое исправление (если установка почти завершена)

Если большинство пакетов установлено, можно попробовать:

```bash
# 1. Удалить проблемные конфигурации Prosody
rm -rf /etc/prosody/conf.avail/*meet*
rm -rf /var/lib/prosody/*meet*

# 2. Переустановить только prosody модуль
apt remove --purge jitsi-meet-prosody
apt install --reinstall jitsi-meet-prosody

# При запросе домена введите: meet.super2026.online (БЕЗ КАВЫЧЕК!)

# 3. Завершить конфигурацию остальных пакетов
dpkg --configure -a
```

---

## ✅ Проверка после исправления

```bash
# Проверить статус сервисов
systemctl status prosody
systemctl status jitsi-videobridge2
systemctl status jicofo
systemctl status nginx

# Проверить, что конфигурация правильная
cat /etc/prosody/conf.avail/meet.super2026.online.cfg.lua | grep -i "VirtualHost"
# Должно быть: VirtualHost "meet.super2026.online" (с обычными кавычками в файле)
```

---

**Дата создания:** 2025-01-28

