# ⚡ БЫСТРЫЙ СТАРТ: Установка собственного Jitsi сервера

## 🎯 Цель
Заменить тестовый публичный сервер `meet.jit.si` на ваш собственный сервер `meet.super2026.online`

---

## 📋 ЧТО НУЖНО СДЕЛАТЬ (пошагово)

### 1️⃣ Подготовка DNS (5 минут)

В панели управления доменом `super2026.online` добавьте **A-запись**:

```
Тип: A
Имя: meet
Значение: 5.129.223.23
TTL: 3600
```

Подождите 5-15 минут, затем проверьте:
```bash
nslookup meet.super2026.online
```
Должен вернуть: `5.129.223.23`

---

### 2️⃣ Установка Jitsi на сервере (15-20 минут)

Подключитесь к серверу:
```bash
ssh root@5.129.223.23
```

Выполните команды по порядку:

```bash
# Обновление системы
apt update && apt upgrade -y

# Установка необходимых пакетов
apt install -y nginx certbot python3-certbot-nginx ufw

# Добавление репозитория Jitsi
echo 'deb https://download.jitsi.org stable/' | sudo tee /etc/apt/sources.list.d/jitsi-stable.list
wget -qO - https://download.jitsi.org/jitsi-key.gpg.key | sudo apt-key add -
apt update

# Установка Jitsi Meet
apt install -y jitsi-meet
```

**Во время установки:**
- Введите домен: `meet.super2026.online`
- Выберите: **"Generate a new self-signed certificate"**

---

### 3️⃣ Настройка SSL (5 минут)

```bash
# Генерация Let's Encrypt сертификата
/usr/share/jitsi-meet/scripts/install-letsencrypt-cert.sh
```

Введите домен: `meet.super2026.online`

---

### 4️⃣ Настройка файрвола (2 минуты)

```bash
ufw allow 80/tcp
ufw allow 443/tcp
ufw allow 10000/udp
ufw allow 4443/tcp
ufw allow OpenSSH
ufw enable
```

---

### 5️⃣ Отключение водяных знаков (опционально, 2 минуты)

```bash
nano /etc/jitsi/meet/meet.super2026.online-interface_config.js
```

Найдите и измените на:
```javascript
SHOW_JITSI_WATERMARK: false,
SHOW_BRAND_WATERMARK: false,
SHOW_POWERED_BY: false,
```

Сохраните: Ctrl+O, Enter, Ctrl+X

```bash
# Перезапуск
systemctl restart jitsi-videobridge2 jicofo prosody nginx
```

---

### 6️⃣ Проверка работы Jitsi (2 минуты)

Откройте в браузере: `https://meet.super2026.online`

Создайте тестовую комнату и проверьте видео/аудио.

---

### 7️⃣ Обновление кода приложения (5 минут)

**Код уже обновлен!** ✅

Теперь нужно добавить переменную окружения:

#### На сервере:
```bash
cd /root/cosmos-of-hopes
nano .env.local
```

Добавьте строку:
```env
NEXT_PUBLIC_JITSI_SERVER_URL=https://meet.super2026.online
```

Сохраните (Ctrl+O, Enter, Ctrl+X)

#### Пересоберите приложение:
```bash
git pull origin main
npm run build
pm2 restart cosmos-of-hopes --update-env
```

---

## ✅ ГОТОВО!

Теперь ваше приложение использует собственный Jitsi сервер!

---

## 🔍 Проверка

1. Откройте: `https://super2026.online`
2. Зайдите в любую комнату
3. Откройте видеокомнату
4. В консоли браузера (F12) проверьте, что URL iframe содержит `meet.super2026.online`

---

## 📚 Подробные инструкции

- **Полная установка:** `JITSI_SERVER_SETUP.md`
- **Обновление кода:** `JITSI_CODE_UPDATE.md`

---

## ❓ Проблемы?

### Jitsi не запускается:
```bash
systemctl status jitsi-videobridge2
journalctl -u jitsi-videobridge2 -n 50
```

### SSL не работает:
```bash
certbot certificates
```

### Проверка портов:
```bash
netstat -tulpn | grep -E ':(80|443|10000|4443)'
```

---

**Общее время:** ~30-40 минут  
**Дата:** 2025-01-28

