# 🔗 Настройка ссылок для приглашений

## Проблема

По умолчанию ссылки генерируются с `localhost`, что не работает для реальных пользователей.

## Решение

### Вариант 1: Добавить переменную окружения (рекомендуется)

1. Откройте файл `.env.local` в папке `cosmos-of-hopes/`
2. Добавьте строку с вашим реальным URL:

```env
NEXT_PUBLIC_APP_URL=https://ваш-домен.com
```

**Примеры:**
- Если деплоите на Vercel: `NEXT_PUBLIC_APP_URL=https://cosmos-of-hopes.vercel.app`
- Если деплоите на Netlify: `NEXT_PUBLIC_APP_URL=https://cosmos-of-hopes.netlify.app`
- Если у вас свой домен: `NEXT_PUBLIC_APP_URL=https://cosmosofhopes.com`

3. Перезапустите dev сервер:
```bash
npm run dev
```

### Вариант 2: Временное решение для тестирования

Если нужно протестировать прямо сейчас, можно использовать:
- **ngrok** для создания публичного туннеля к localhost
- **Cloudflare Tunnel** (бесплатно)
- Или просто деплой на Vercel/Netlify (бесплатно)

## Где взять URL для деплоя?

### Vercel (рекомендуется, бесплатно):
1. Зарегистрируйтесь на [vercel.com](https://vercel.com)
2. Подключите GitHub репозиторий
3. Vercel автоматически даст вам URL типа `https://cosmos-of-hopes.vercel.app`
4. Добавьте этот URL в `.env.local` как `NEXT_PUBLIC_APP_URL`

### Netlify (бесплатно):
1. Зарегистрируйтесь на [netlify.com](https://netlify.com)
2. Подключите репозиторий
3. Получите URL типа `https://cosmos-of-hopes.netlify.app`

### ngrok (для локального тестирования):
```bash
# Установите ngrok
npm install -g ngrok

# Запустите туннель к localhost:3000
ngrok http 3000

# Используйте полученный URL (например, https://abc123.ngrok.io)
```

## Проверка

После настройки `NEXT_PUBLIC_APP_URL`, ссылки в компоненте `InviteLink` будут использовать реальный URL вместо localhost.

## Важно

- ⚠️ **НЕ коммитьте** `.env.local` в git (он уже в .gitignore)
- ✅ Переменная `NEXT_PUBLIC_APP_URL` должна начинаться с `https://`
- ✅ Не добавляйте `/` в конце URL
- ✅ После изменения `.env.local` нужно перезапустить dev сервер
