# 🌌 КОСМОС НАДЕЖД

Новогодний проект, где пользователи создают игрушки на общую виртуальную ёлку, которые 1 января превращаются в звёзды и улетают в общий космос желаний.

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
npm install
```

### 2. Настройка Supabase

1. Создайте аккаунт на [supabase.com](https://supabase.com)
2. Создайте новый проект
3. Скопируйте `.env.local.example` в `.env.local`
4. Заполните значения:
   - `NEXT_PUBLIC_SUPABASE_URL` - URL вашего проекта Supabase
   - `NEXT_PUBLIC_SUPABASE_ANON_KEY` - Anon key из настроек проекта
   - `NEXT_PUBLIC_APP_URL` - **Ваш production URL** (например, `https://cosmos-of-hopes.vercel.app` или ваш домен)
     - ⚠️ **ВАЖНО**: Без этого ссылки приглашений будут указывать на localhost!
     - В development можно оставить пустым - будет использоваться localhost

### 3. Создание таблиц в Supabase

Выполните SQL скрипты из `supabase/schema.sql` в SQL Editor Supabase.

### 4. Запуск приложения

```bash
npm run dev
```

Приложение будет доступно на `http://localhost:3000`

## 📁 Структура проекта

```
cosmos-of-hopes/
├── app/                    # Next.js App Router
│   ├── page.tsx           # Главная страница
│   └── ...
├── components/            # React компоненты
│   ├── constructor/      # Конструктор игрушек
│   ├── tree/              # 3D ёлка
│   ├── cosmos/            # Космос со звездами
│   └── ...
├── lib/                   # Утилиты и сервисы
│   ├── supabase.ts        # Supabase клиент
│   └── ...
├── types/                 # TypeScript типы
│   ├── toy.ts
│   ├── room.ts
│   └── user.ts
└── supabase/              # Supabase функции и схемы
    ├── functions/         # Edge Functions
    └── schema.sql         # SQL схема
```

## 🛠️ Технологии

- **Next.js 14** - React фреймворк
- **TypeScript** - Типизация
- **Three.js** - 3D графика
- **Supabase** - Backend (PostgreSQL, Storage, Edge Functions)
- **Telegram Mini App** - Платформа распространения

## 📝 TODO

См. файл `COSMOS_OF_HOPES_TECHNICAL_SPEC.md` для полного технического задания.
