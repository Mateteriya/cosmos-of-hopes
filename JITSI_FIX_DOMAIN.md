# 🔧 Исправление домена с обратными кавычками

## Проблема
Домен сохранен с обратными кавычками `meet.super2026.online` вместо обычного текста.

---

## 🔍 Шаг 1: Найти где сохранен домен

Выполните на сервере:

```bash
# Поиск в конфигурационных файлах
grep -r "meet.super2026.online" /etc/jitsi/
grep -r "meet.super2026.online" /etc/prosody/

# Проверить переменные окружения (если есть)
env | grep -i meet
env | grep -i jitsi
```

---

## 🛠️ Шаг 2: Найти основной конфигурационный файл

```bash
# Проверить главный конфиг Jitsi
cat /etc/jitsi/meet/meet.super2026.online-config.js 2>/dev/null || echo "Файл не найден"

# Проверить переменную домена (если есть)
cat /etc/jitsi/videobridge/config 2>/dev/null | grep -i domain || echo "Не найдено"
cat /etc/jitsi/jicofo/config 2>/dev/null | grep -i domain || echo "Не найдено"
```

---

## 🔧 Шаг 3: Исправить домен вручную

Скорее всего домен сохранен в каком-то конфигурационном файле. Нужно:

### Вариант A: Если есть файл с доменом

```bash
# Найти все файлы с доменом
find /etc/jitsi -type f -exec grep -l "meet.super2026.online" {} \;
find /etc/prosody -type f -exec grep -l "meet.super2026.online" {} \;

# Заменить обратные кавычки на обычные (БЕЗ кавычек вообще в файлах конфигурации)
# Но сначала посмотрим что там
```

### Вариант B: Полная очистка и переустановка с правильным доменом

```bash
# 1. Удалить ВСЕ конфигурации Jitsi
rm -rf /etc/jitsi
rm -rf /etc/prosody/conf.avail/*meet*
rm -rf /var/lib/prosody/*meet*

# 2. Удалить все пакеты Jitsi
apt remove --purge jitsi-meet jitsi-meet-prosody jitsi-meet-web jitsi-meet-web-config jitsi-meet-turnserver jicofo jitsi-videobridge2

# 3. Установить заново с правильным доменом через переменную окружения
export JITSI_MEET_DOMAIN=meet.super2026.online
apt install -y jitsi-meet
```

---

## 🎯 Правильный способ установки с доменом

Если пакет `jitsi-meet` спрашивает домен интерактивно, нужно:

1. **Установить пакет в неинтерактивном режиме:**
```bash
# Установить переменную окружения
export JITSI_MEET_DOMAIN=meet.super2026.online

# Установить пакет (он должен использовать переменную)
DEBIAN_FRONTEND=noninteractive apt install -y jitsi-meet
```

2. **Или установить через debconf:**
```bash
# Установить домен через debconf
echo "jitsi-meet jitsi-videobridge/jvb-hostname string meet.super2026.online" | debconf-set-selections
apt install -y jitsi-meet
```

---

## 🔍 Быстрая диагностика

Выполните эти команды чтобы понять, где проблема:

```bash
# 1. Проверить все файлы с доменом
grep -r "meet.super2026.online" /etc/ 2>/dev/null | head -20

# 2. Проверить переменные debconf
debconf-show jitsi-meet 2>/dev/null | grep -i domain

# 3. Проверить ключи Prosody (обратите внимание на кавычки)
ls -la /var/lib/prosody/*meet* 2>/dev/null
```

---

**Дата создания:** 2025-01-28

