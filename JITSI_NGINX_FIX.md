# 🔧 Исправление конфигурации Nginx для Jitsi

## Проблема
Nginx не может загрузить SSL сертификат из-за обратных кавычек в пути:
```
cannot load certificate "/etc/jitsi/meet/`meet.super2026.online`.crt"
```

## Решение

### Шаг 1: Найти файл конфигурации Jitsi

```bash
# Найти файл конфигурации
ls -la /etc/nginx/sites-enabled/*meet*

# Или
find /etc/nginx -name "*meet*" -type f
```

### Шаг 2: Исправить конфигурацию

Файл скорее всего: `/etc/nginx/sites-enabled/meet.super2026.online.conf`

```bash
# Открыть файл
nano /etc/nginx/sites-enabled/meet.super2026.online.conf
```

Или если имя файла с обратными кавычками:

```bash
# Посмотреть все файлы
ls -la /etc/nginx/sites-enabled/

# Если видите файл с обратными кавычками, переименовать его или исправить содержимое
```

### Шаг 3: Исправить пути к сертификатам

В файле конфигурации найдите строки типа:
```
ssl_certificate /etc/jitsi/meet/`meet.super2026.online`.crt;
ssl_certificate_key /etc/jitsi/meet/`meet.super2026.online`.key;
```

И замените на:
```
ssl_certificate /etc/jitsi/meet/meet.super2026.online.crt;
ssl_certificate_key /etc/jitsi/meet/meet.super2026.online.key;
```

**ВАЖНО:** Убрать обратные кавычки!

### Шаг 4: Альтернативный способ - пересоздать конфигурацию Jitsi

Если файл слишком сложный, можно удалить и пересоздать:

```bash
# Удалить неправильный файл
rm /etc/nginx/sites-enabled/*meet*

# Переустановить конфигурацию nginx для Jitsi
dpkg-reconfigure jitsi-meet-web-config
```

Или переустановить весь пакет jitsi-meet:

```bash
apt remove --purge jitsi-meet-web-config
apt install jitsi-meet-web-config
```

### Шаг 5: Проверить и перезагрузить

```bash
# Проверить конфигурацию
nginx -t

# Если OK, перезагрузить
systemctl reload nginx

# Проверить статус
systemctl status nginx
```

---

**Дата:** 2025-01-28

