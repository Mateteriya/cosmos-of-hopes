# 📚 ПОДРОБНАЯ ИНСТРУКЦИЯ ПО НАСТРОЙКЕ SUPABASE

## ✅ ШАГ 1: Выполнить SQL схему (создание таблиц)

### Что это делает:
Создает все необходимые таблицы в базе данных для хранения игрушек, комнат и пользователей.

### Как это сделать:

1. **Откройте ваш проект Supabase:**
   - Перейдите на [supabase.com](https://supabase.com)
   - Войдите в свой аккаунт
   - Найдите и откройте проект, который вы только что создали

2. **Откройте SQL Editor:**
   - В левом меню найдите раздел **"SQL Editor"** (или "SQL редактор")
   - Нажмите на него
   - Вы увидите пустое окно с текстом "New query" или "Новый запрос"

3. **Откройте файл со схемой:**
   - На вашем компьютере откройте файл: `cosmos-of-hopes/supabase/schema.sql`
   - Выделите ВСЁ содержимое файла (Ctrl+A)
   - Скопируйте (Ctrl+C)

4. **Вставьте SQL в редактор:**
   - Вернитесь в браузер с Supabase
   - В окне SQL Editor нажмите в пустое поле
   - Вставьте скопированный текст (Ctrl+V)
   - Вы увидите много строк SQL кода

5. **Запустите SQL:**
   - В правом нижнем углу окна SQL Editor найдите кнопку **"Run"** (или "Выполнить")
   - Или нажмите клавиши **Ctrl+Enter**
   - Подождите несколько секунд

6. **Проверьте результат:**
   - Внизу окна должно появиться сообщение об успехе (зеленое)
   - Если есть ошибки (красное сообщение) - скопируйте текст ошибки и сообщите мне

### Что должно получиться:
После выполнения SQL в вашей базе данных создадутся 4 таблицы:
- `toys` - для хранения игрушек
- `rooms` - для персональных комнат
- `user_profiles` - для профилей пользователей
- `room_members` - для участников комнат

---

## ✅ ШАГ 2: Создать Storage bucket (хранилище для изображений)

### Что это делает:
Создает место для хранения загруженных изображений (фото игрушек и фото пользователей).

### Как это сделать:

#### Часть 1: Создание bucket

1. **Откройте раздел Storage:**
   - В левом меню Supabase найдите раздел **"Storage"** (или "Хранилище")
   - Нажмите на него

2. **Создайте новый bucket:**
   - Вы увидите страницу с надписью "Buckets" (или "Контейнеры")
   - Найдите кнопку **"New bucket"** (или "Создать bucket" / "Новый контейнер")
   - Обычно она находится в правом верхнем углу или в центре страницы
   - Нажмите на эту кнопку

3. **Заполните форму:**
   - **Name** (Название): введите `toy-images` (обязательно именно так, без пробелов!)
   - **Public bucket** (Публичный доступ): **ВКЛЮЧИТЕ** переключатель (должен быть зеленым/синим)
     - Это важно! Без этого изображения не будут доступны для просмотра
   - Остальные поля можно оставить по умолчанию

4. **Создайте bucket:**
   - Нажмите кнопку **"Create bucket"** (или "Создать")
   - Подождите несколько секунд
   - Вы должны увидеть новый bucket с названием `toy-images` в списке

#### Часть 2: Настройка политик доступа (важно!)

Без этого шага загрузка изображений не будет работать.

1. **Откройте SQL Editor снова:**
   - Вернитесь в раздел **"SQL Editor"** (как в шаге 1)

2. **Создайте новый запрос:**
   - Нажмите кнопку **"New query"** (или "Новый запрос") если нужно
   - Очистите поле (если там что-то есть)

3. **Скопируйте и вставьте этот SQL код:**

```sql
-- Политики для Storage bucket toy-images

-- Разрешаем всем загружать изображения
CREATE POLICY "Anyone can upload images" 
ON storage.objects
FOR INSERT 
WITH CHECK (bucket_id = 'toy-images');

-- Разрешаем всем читать изображения
CREATE POLICY "Anyone can read images" 
ON storage.objects
FOR SELECT 
USING (bucket_id = 'toy-images');

-- Разрешаем всем удалять свои изображения (опционально)
CREATE POLICY "Anyone can delete their images" 
ON storage.objects
FOR DELETE 
USING (bucket_id = 'toy-images');
```

4. **Запустите SQL:**
   - Нажмите кнопку **"Run"** (или Ctrl+Enter)
   - Должно появиться сообщение об успехе

### Проверка:
- В разделе Storage вы должны видеть bucket `toy-images`
- У него должен быть значок "Public" (публичный)

---

## ✅ ШАГ 3: Получение API ключей (если еще не получили)

### Где найти ключи:

1. **Откройте Settings (Настройки):**
   - В левом меню найдите раздел **"Settings"** (или "Настройки")
   - Нажмите на него

2. **Откройте раздел API:**
   - В настройках найдите подраздел **"API"**
   - Нажмите на него

3. **Скопируйте ключи:**
   - Найдите раздел **"Project URL"** (URL проекта)
     - Скопируйте этот адрес (например: `https://xxxxx.supabase.co`)
     - Это ваш `NEXT_PUBLIC_SUPABASE_URL`
   
   - Найдите раздел **"Project API keys"** (API ключи проекта)
     - Найдите ключ с пометкой **"anon"** или **"public"** (не "service_role"!)
     - Скопируйте этот ключ (длинная строка)
     - Это ваш `NEXT_PUBLIC_SUPABASE_ANON_KEY`

---

## ✅ ШАГ 4: Создание файла .env.local

### Что это:
Файл с секретными ключами, который приложение использует для подключения к Supabase.

### Как создать:

1. **Откройте папку проекта:**
   - Перейдите в папку `cosmos-of-hopes`
   - Это должно быть: `C:\Projects\NewDreamer\cosmos-of-hopes`

2. **Создайте новый файл:**
   - В этой папке создайте новый файл с названием `.env.local`
   - **Важно:** файл должен начинаться с точки!
   - Если Windows не дает создать файл с точкой в начале:
     - Создайте файл `env.local` (без точки)
     - Потом переименуйте его в `.env.local`
     - Или используйте текстовый редактор (Notepad++, VS Code)

3. **Вставьте в файл:**
   Откройте файл `.env.local` и вставьте туда:

```env
NEXT_PUBLIC_SUPABASE_URL=ваш_project_url_здесь
NEXT_PUBLIC_SUPABASE_ANON_KEY=ваш_anon_key_здесь
```

**Замените:**
- `ваш_project_url_здесь` на URL, который вы скопировали из Supabase
- `ваш_anon_key_здесь` на ключ, который вы скопировали из Supabase

**Пример того, как должно выглядеть:**
```env
NEXT_PUBLIC_SUPABASE_URL=https://abcdefghijklmnop.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImFiY2RlZmdoaWprbG1ub3AiLCJyb2xlIjoiYW5vbiIsImlhdCI6MTYzODk2NzI5MCwiZXhwIjoxOTU0NTQzMjkwfQ.abcdefghijklmnopqrstuvwxyz1234567890
```

4. **Сохраните файл:**
   - Сохраните файл (Ctrl+S)
   - Закройте его

---

## ✅ ШАГ 5: Проверка настройки

### Как проверить, что всё работает:

1. **Запустите приложение:**
   ```bash
   cd cosmos-of-hopes
   npm run dev
   ```

2. **Откройте в браузере:**
   - Перейдите на [http://localhost:3000](http://localhost:3000)

3. **Попробуйте создать игрушку:**
   - Заполните форму конструктора
   - Нажмите "Повесить на ёлку"
   - Если всё работает - вы увидите сообщение об успехе

4. **Проверьте в Supabase:**
   - Откройте Supabase → **Table Editor** (Редактор таблиц)
   - Выберите таблицу **`toys`**
   - Вы должны увидеть вашу созданную игрушку!
   - Откройте **Storage** → **toy-images**
   - Вы должны увидеть загруженные изображения!

---

## ❌ ЧАСТЫЕ ПРОБЛЕМЫ И РЕШЕНИЯ

### Проблема 1: "Ошибка при выполнении SQL"
**Решение:**
- Убедитесь, что скопировали ВСЁ содержимое файла `schema.sql`
- Проверьте, что в SQL Editor нет других запросов
- Попробуйте выполнить SQL по частям (скопируйте первую часть, выполните, потом вторую)

### Проблема 2: "Bucket уже существует"
**Решение:**
- Это нормально, если вы уже создали bucket
- Просто пропустите создание bucket и перейдите к настройке политик

### Проблема 3: "Ошибка загрузки изображения"
**Решение:**
- Проверьте, что bucket `toy-images` создан и помечен как Public
- Проверьте, что политики доступа созданы (шаг 2, часть 2)
- Попробуйте выполнить SQL для политик еще раз

### Проблема 4: "Не могу создать файл .env.local"
**Решение:**
- Используйте VS Code или другой редактор кода
- Или создайте файл `env.local` и переименуйте его через командную строку:
  ```bash
  cd cosmos-of-hopes
  ren env.local .env.local
  ```

### Проблема 5: "Приложение не подключается к Supabase"
**Решение:**
- Проверьте, что файл `.env.local` создан в правильной папке (`cosmos-of-hopes`)
- Проверьте, что ключи скопированы правильно (без лишних пробелов)
- Перезапустите приложение (остановите Ctrl+C и запустите снова `npm run dev`)

---

## 📞 ЕСЛИ ЧТО-ТО НЕ РАБОТАЕТ

Если у вас возникли проблемы:

1. **Скопируйте текст ошибки** (если есть)
2. **Сделайте скриншот** (если возможно)
3. **Опишите, на каком шаге** возникла проблема
4. **Сообщите мне** - я помогу разобраться!

---

## ✅ ЧЕКЛИСТ ПРОВЕРКИ

Проверьте, что вы выполнили все шаги:

- [ ] SQL схема выполнена (таблицы созданы)
- [ ] Bucket `toy-images` создан
- [ ] Bucket помечен как Public
- [ ] Политики доступа для Storage созданы
- [ ] API ключи скопированы
- [ ] Файл `.env.local` создан
- [ ] Ключи вставлены в `.env.local`
- [ ] Приложение запускается без ошибок
- [ ] Можно создать игрушку
- [ ] Игрушка сохраняется в базу данных
- [ ] Изображения загружаются в Storage

Если все пункты отмечены - **поздравляю, настройка завершена!** 🎉

---

**Готово! Теперь можно продолжать разработку!** 🚀


