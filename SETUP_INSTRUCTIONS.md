# 📋 ИНСТРУКЦИЯ ПО НАСТРОЙКЕ

## Шаг 1: Создание проекта Supabase

1. Перейдите на [supabase.com](https://supabase.com)
2. Создайте аккаунт (если нет)
3. Создайте новый проект:
   - Название: `cosmos-of-hopes` (или любое другое)
   - Пароль базы данных: **сохраните его!**
   - Регион: выберите ближайший

## Шаг 2: Настройка базы данных

1. В проекте Supabase откройте **SQL Editor**
2. Скопируйте содержимое файла `supabase/schema.sql`
3. Вставьте в SQL Editor и нажмите **Run**
4. Проверьте, что таблицы созданы:
   - `toys`
   - `rooms`
   - `user_profiles`
   - `room_members`

## Шаг 3: Настройка Storage (хранилище изображений)

1. В проекте Supabase откройте **Storage**
2. Нажмите **Create a new bucket**
3. Название: `toy-images`
4. Публичный доступ: **Включить** (Public bucket)
5. Нажмите **Create bucket**
6. Откройте настройки bucket и добавьте политику:

```sql
-- Политика для загрузки изображений (в SQL Editor)
CREATE POLICY "Anyone can upload images" ON storage.objects
FOR INSERT WITH CHECK (bucket_id = 'toy-images');

CREATE POLICY "Anyone can read images" ON storage.objects
FOR SELECT USING (bucket_id = 'toy-images');
```

## Шаг 4: Получение ключей API

1. В проекте Supabase откройте **Settings** → **API**
2. Скопируйте:
   - **Project URL** → это `NEXT_PUBLIC_SUPABASE_URL`
   - **anon public** key → это `NEXT_PUBLIC_SUPABASE_ANON_KEY`

## Шаг 5: Настройка переменных окружения

1. В папке `cosmos-of-hopes` создайте файл `.env.local`
2. Добавьте:

```env
NEXT_PUBLIC_SUPABASE_URL=ваш_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=ваш_anon_key
```

## Шаг 6: Запуск приложения

```bash
cd cosmos-of-hopes
npm run dev
```

Откройте [http://localhost:3000](http://localhost:3000)

## ✅ Проверка

1. Откройте приложение
2. Создайте тестовую игрушку
3. Проверьте в Supabase:
   - Таблица `toys` должна содержать новую запись
   - Storage `toy-images` должен содержать загруженные изображения

## 🚀 Следующие шаги

После настройки можно:
1. Создать 3D ёлку для отображения игрушек
2. Настроить Edge Functions для уведомлений
3. Интегрировать Telegram Mini App

---

**Вопросы?** Смотрите `COSMOS_OF_HOPES_TECHNICAL_SPEC.md` для полной документации.


