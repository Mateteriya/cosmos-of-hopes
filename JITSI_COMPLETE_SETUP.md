# ✅ Завершение установки Jitsi

## 🎉 Отлично! Основная часть установлена

Домен теперь правильный, сертификаты созданы. Осталось завершить установку остальных компонентов.

---

## 📋 Шаг 1: Завершить установку остальных пакетов

Выполните на сервере:

```bash
# Установить остальные пакеты Jitsi
apt install -y jitsi-meet jitsi-meet-turnserver

# Завершить конфигурацию
dpkg --configure -a
apt install -f
```

---

## 🔍 Шаг 2: Проверить статус сервисов

```bash
# Проверить статус всех сервисов Jitsi
systemctl status prosody
systemctl status jitsi-videobridge2
systemctl status jicofo
systemctl status nginx

# Если jitsi-videobridge2 не запущен, проверить причину
journalctl -xeu jitsi-videobridge2.service -n 50
```

---

## 🔒 Шаг 3: Установить SSL сертификат Let's Encrypt

```bash
# Установить SSL сертификат
/usr/share/jitsi-meet/scripts/install-letsencrypt-cert.sh

# Введите ваш email когда попросит
# Домен должен подхватиться автоматически
```

---

## 🔥 Шаг 4: Настроить файрвол (если еще не сделано)

```bash
ufw allow 80/tcp
ufw allow 443/tcp
ufw allow 10000/udp
ufw allow 4443/tcp
ufw allow OpenSSH
ufw enable
```

---

## ✅ Шаг 5: Проверить работу

1. Откройте в браузере: `https://meet.super2026.online`
2. Создайте тестовую комнату (любое имя)
3. Проверьте работу видео/аудио

---

## 🐛 Если jitsi-videobridge2 не запускается

```bash
# Проверить логи
journalctl -xeu jitsi-videobridge2.service -n 100

# Проверить конфигурацию
cat /etc/jitsi/videobridge/config

# Если нужно, перезапустить
systemctl restart jitsi-videobridge2
systemctl status jitsi-videobridge2
```

---

## 📝 Что дальше?

После успешной установки:
1. ✅ Обновить код приложения (добавить переменную окружения)
2. ✅ Пересобрать и перезапустить приложение
3. ✅ Протестировать видеокомнаты

---

**Дата:** 2025-01-28

