# ✅ Финальные шаги установки Jitsi

## ✅ Что уже готово
- ✅ Nginx работает
- ✅ Prosody установлен
- ✅ Сертификаты созданы
- ✅ Конфигурация исправлена

---

## 📋 Шаг 1: Завершить установку остальных пакетов

Выполните на сервере:

```bash
# Установить остальные пакеты Jitsi
apt install -y jitsi-meet jitsi-meet-turnserver

# Завершить конфигурацию
dpkg --configure -a
apt install -f
```

---

## 🔒 Шаг 2: Установить SSL сертификат Let's Encrypt

После установки всех пакетов:

```bash
# Установить Let's Encrypt сертификат
/usr/share/jitsi-meet/scripts/install-letsencrypt-cert.sh
```

Введите ваш email (например: `Mateteriya@gmail.com`), домен подхватится автоматически.

Это заменит самоподписанные сертификаты на настоящие SSL сертификаты.

---

## 🔥 Шаг 3: Настроить файрвол (если еще не сделано)

```bash
ufw allow 80/tcp
ufw allow 443/tcp
ufw allow 10000/udp
ufw allow 4443/tcp
ufw allow OpenSSH
ufw enable
```

---

## ✅ Шаг 4: Проверить работу сервисов

```bash
# Проверить статус всех сервисов
systemctl status prosody
systemctl status jitsi-videobridge2
systemctl status jicofo
systemctl status nginx

# Если jitsi-videobridge2 не запущен, проверить
journalctl -xeu jitsi-videobridge2.service -n 50
```

---

## 🌐 Шаг 5: Проверить работу Jitsi

1. Откройте в браузере: `https://meet.super2026.online`
2. Создайте тестовую комнату (любое имя, например: `test123`)
3. Разрешите доступ к микрофону/камере
4. Проверьте работу видео/аудио

---

## 🔄 Шаг 6: Обновить код приложения

После успешной проверки работы Jitsi:

1. Добавить переменную окружения на сервере:

```bash
cd /root/cosmos-of-hopes
nano .env.local
```

Добавьте строку:
```env
NEXT_PUBLIC_JITSI_SERVER_URL=https://meet.super2026.online
```

2. Пересобрать и перезапустить приложение:

```bash
git pull origin main
npm run build
pm2 restart cosmos-of-hopes --update-env
```

---

## 🎉 Готово!

Теперь ваше приложение использует собственный Jitsi сервер!

---

**Дата:** 2025-01-28

